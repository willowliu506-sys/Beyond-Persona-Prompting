\chapter{Materials and Methods} \label{Chap3}

\section{Research Questions and Overall Experimental Framework}

\subsection{Research Questions and Study Mapping}

The overall research question guiding this study is:

\begin{quote}
\small
\textbf{Main RQ:} How can selected ADHD-related learning characteristics be represented in LLM-based student simulation, and to what extent does a cognitive-process-based representation produce more theory-consistent task behaviour than persona prompting while retaining its constraint-related patterns under additional response-stage learner attributes?
\end{quote}

This overarching question is addressed through three research questions with distinct evidential roles:

\begin{quote}
\small
\textbf{RQ1---Mechanism:} Does the CPB processing pipeline execute its intended rules and produce traceable transitions from instructional information, through attention, working-memory processing and encoding, to Explicit LTM?

\textbf{RQ2---Representation:} Compared with persona prompting, does CPB produce learning behaviour that shows more theoretically consistent sensitivity to controlled distraction and instructional processing demand?

\textbf{RQ3---Constraint-Pattern Retention under Attribute Additions:} When Language Ability and Big-Five prompt dimensions are added at the response stage, how do assessment outcomes change, and do CPB's previously established constraint-related aggregate patterns remain observable?
\end{quote}

\begin{table}[H]
\centering
\caption{Mapping of research questions to experimental studies and evidence.}
\label{tab:rq-study-mapping}
\small
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{p{0.06\textwidth} p{0.20\textwidth} p{0.39\textwidth} p{0.28\textwidth}}
\toprule
\textbf{RQ} & \textbf{Evidential role} & \textbf{Study and core comparison} & \textbf{Primary evidence} \\
\midrule
\textbf{RQ1} & Mechanistic validity and process execution & \textbf{Study 1:} factorial ablation of Attention and Working-Memory mechanisms within CPB & Mechanism-execution fidelity; stage-specific information-state transitions; cumulative propagation to the final Explicit LTM \\
\textbf{RQ2} & Comparative representational validity & \textbf{Study 2:} Prompt-based and CPB-based learner conditions compared under clean and distracted instructional materials & Within-representation performance differentiation; sensitivity to controlled distraction and instructional processing demand \\
\textbf{RQ3} & Response-stage attribute extension and aggregate-pattern retention & \textbf{Study 3:} Study 2 learner conditions extended with A1--B2 Language Ability and Big-Five prompt dimensions under clean and distracted materials & Baseline-relative outcome changes; whether previously established constraint-related aggregate patterns remain observable \\
\bottomrule
\end{tabular}
\end{table}

The three research questions form a progressive evidential sequence. Study 1 first establishes whether the internal CPB mechanisms execute as intended, produce stage-specific information-state changes, and propagate these changes to the final Explicit LTM. Study 2 then compares Prompt-based and CPB-based learner representations in terms of within-representation performance differentiation and behavioural sensitivity to controlled distraction and instructional processing demand. Study 3 introduces Language Ability and Big-Five prompt dimensions at the response stage, describes the resulting outcome changes, and checks whether the constraint-related aggregate patterns previously established for CPB remain observable.

\subsection{Overall Research Workflow}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,keepaspectratio]{Figures/Chapter3/export/ch03_overall_research_workflow.png}
    \caption{Overall workflow of the Teaching and Assessment Phases.}
    \label{fig:overall-research-workflow}
\end{figure}

All three studies share a common \textbf{Teaching/Learning Phase} in which instructional materials are processed into a frozen post-exposure information state. Study 1 terminates at the resulting Explicit LTM and validates only the internal mechanism execution and information-state transitions. Studies 2 and 3 additionally proceed to an \textbf{Assessment Phase}, as illustrated in Figure~\ref{fig:overall-research-workflow}, so that the behavioural consequences of the learner representations can be compared without treating answer performance as evidence for RQ1.

During the \textbf{Teaching/Learning Phase}, frozen instructional materials are presented sequentially to the simulated learner and processed according to the corresponding learner representation. This process produces a \textbf{frozen post-exposure information state} that represents the information available after instruction. In Study 1, this state is the terminal analysis object. In Studies 2 and 3, it is frozen before assessment so that subsequent questions do not alter the learned information available to the learner.

For Studies 2 and 3, each \textbf{Assessment Phase} question is answered independently from the same frozen post-exposure state. Each question therefore forms a separate branch from the completed learning phase, preventing earlier questions or generated answers from providing additional information to later questions. The resulting student answers are then evaluated using the same criterion-level scoring framework, providing a common behavioural outcome for the representational comparisons in RQ2 and RQ3.

This design separates \textbf{learner-state formation during instruction} from \textbf{observable assessment behaviour}. RQ1 is restricted to the former and ends at Explicit LTM, whereas RQ2 and RQ3 use both phases to examine assessment behaviour after the learning state has been frozen.

The remainder of Chapter 3 specifies the methodological components required to operationalise and evaluate this framework. Section 3.2 first establishes the shared experimental resources used across all studies, including the instructional materials and preprocessing procedure, the operationalisation of instructional processing demand, the assessment questions, and the criterion-level scoring framework. Section 3.3 then defines the two learner-representation approaches compared in this thesis---persona prompting and the Cognitive-Process-Based (CPB) framework---and clarifies how they differ in learner-characteristic representation, knowledge-state formation, and answer-stage information access. Building on this comparison, Section 3.4 details the CPB mechanisms themselves, including the Attention Filter, Working-Memory processing capacity, and shared knowledge-encoding procedure. Section 3.5 then translates the three research questions into Studies 1--3, specifying the corresponding experimental conditions, comparisons, and evaluation measures for mechanistic validity, comparative representational validity, and multidimensional attribute extension. Finally, Section 3.6 documents the model and generation settings, reproducibility procedures, statistical and repetition conventions, and the ethical scope within which the resulting simulated-learner behaviours are interpreted.

\section{Shared Instructional and Assessment Resources}
\label{sec:teaching-materials-dataset}

\subsection{Source Instructional Materials and Text Preprocessing}
\label{subsec:source-materials}

The instructional corpus comprised seven English-language lessons adapted from Sections 1.1--1.7 of \textit{OpenStax Principles of Finance 2e} \cite{dahlquist2026principlesfinance2e}. The lessons covered the definition and role of finance, the use of data and technology, careers in finance, financial markets and participants, microeconomic and macroeconomic influences, and financial instruments. A single subject area was used to reduce variation attributable to disciplinary genre while retaining a mixture of definitions, factual relations, comparisons, processes, examples, and causal or conditional information suitable for short-answer assessment.

The selected source material was converted into a plain-text instructional format. These sections contained no complex tables or figures, apart from simple process or structural diagrams that could be represented clearly in text. References to source images were replaced with concise textual descriptions of the instructional information they conveyed. This treatment preserved continuity between surrounding passages and avoided leaving incomplete or disconnected content when visual elements were removed from the text-only materials.

The instructional materials and associated assessment resources were finalised and frozen before the formal experiments. The same frozen materials were used across all experimental conditions and repeated runs.

The source summaries were preprocessed before teaching-round segmentation so that each sentence could be interpreted without relying on an antecedent located in a previous round. Context-dependent sentence openings, including unresolved uses of \textit{it}, \textit{they}, and similarly underspecified noun phrases, were replaced with explicit referents where necessary. This operation was restricted to reference resolution: it was not intended to introduce new instructional claims, simplify the underlying finance content, or alter the order in which information was presented.

Following preprocessing, each sentence was reviewed for local interpretability. The review asked whether a student receiving the sentence as part of a short teaching round could identify the subject, relation, and relevant object or outcome without access to an earlier round. The resulting context-independent text formed the clean instructional source from which all experimental conditions were derived.

The preprocessed lessons were divided into teaching rounds containing two or three complete source sentences. Original sentences were not split merely to equalise round length, because syntactic splitting could interrupt a relation or create a fragment that remained difficult to interpret despite coreference resolution. The segmentation procedure instead sought a practical balance between local coherence and moderately comparable round length while preserving source order.

Rounds were assigned identifiers of the form \texttt{R01}, \texttt{R02}, and so forth within each lesson. Sentences were assigned nested identifiers such as \texttt{R01\_S01} and \texttt{R01\_S02}. Each round retained its source sentences in their original order together with a reconstructed instructional text field. The frozen round and sentence identifiers provided a consistent basis for aligning the instructional materials with the experimental resources used in later stages of the study.

\subsection{Instructional Processing-Demand}
\label{subsec:instructional-processing-demand}

This study examines how ADHD-related learner representations process information during controlled classroom learning. The subsequent experiments therefore require a common material-level variable representing the relative processing demand imposed by different Teaching Rounds. This variable serves two functions. First, it provides the shared demand reference for the Working-Memory processing-capacity manipulation described in Section 3.4.2. Second, it provides a continuous material-level variable for analysing whether question performance in Studies 2 and 3 changes systematically with instructional processing demand. A \textbf{Processing Demand Bits (PDB)} value was therefore computed and frozen for every Teaching Round and held constant across all learner conditions.

\subsubsection{Measurement Basis}

Word count alone is insufficient for this purpose. Teaching Rounds of similar length may differ in processing demand because their text differs in predictability; surface length therefore cannot distinguish between instructional inputs that are equally long but differently demanding to process. The present study consequently uses surprisal as the basis of its instructional processing-demand annotation. Surprisal measures how unpredictable a linguistic unit is given its preceding context. For model token $x_t$ at position $t$, surprisal is defined as:

\[
I_\theta(x_t \mid x_{<t})=-\log_2 P_\theta(x_t \mid x_{<t}).
\]

A lower conditional probability indicates that the token is less predictable from its preceding context and consequently has higher surprisal. Because the logarithm uses base 2, surprisal is measured in bits.

Surprisal has long been used in probabilistic accounts of incremental language processing, where less predictable linguistic input is associated with greater processing difficulty \cite{hale-2001-probabilistic,smith-levy-2013-effect}. Large-scale analyses across multiple reading datasets and language-model estimators have continued to find systematic relationships between contextual predictability and reading time \cite{shain-etal-2024-large-scale}. These findings provide a basis for using conditional predictability as a computational proxy for the relative processing demand of instructional text.

The measure is suitable for the present study because Teaching Rounds are organised as continuous, locally coherent instructional inputs. Summing token-level surprisal within a Teaching Round retains two material properties relevant to processing demand: the amount of linguistic input in the round and the difficulty of predicting that input from its within-round context. It therefore provides a richer material-side reference than word count alone. Surprisal does not, however, capture every contributor to cognitive demand, including prior knowledge, discourse comprehension, dependency retrieval, or conceptual complexity. PDB is consequently treated as a \textbf{computational proxy for relative instructional processing demand}, rather than as a direct measure of human cognitive load.

\subsubsection{Round-Level Operationalisation}

Each Teaching Round $R_i$ receives an independent PDB value calculated by summing the surprisal of all model tokens in the round:

\[
D_\theta(R_i)=\sum_{t=1}^{T_i}-\log_2 P_\theta\!\left(x_{i,t}\mid \mathrm{BOS},x_{i,1:t-1}\right),
\]

where $T_i$ is the number of model tokens in Teaching Round $R_i$, and $\mathrm{BOS}$ is an unscored boundary token used to initialise the round. The resulting $D_\theta(R_i)$ is measured in bits and stored in the \texttt{processing\_demand\_bits} field of the frozen instructional-material record.

The primary estimator was GPT-2 small, the approximately 124-million-parameter autoregressive language model identified as \texttt{gpt2} on Hugging Face. GPT-2 directly provides the next-token conditional probabilities required to calculate surprisal and uses byte-level byte-pair encoding, thereby avoiding the out-of-vocabulary problem of traditional word-level models. It can also be frozen and run locally, at a computational cost compatible with repeated verification across the complete instructional corpus. The model and tokenizer were fixed to the same archived version; the complete version identifiers and execution environment were recorded in the experimental reproducibility manifest.

PDB was calculated from the clean instructional text of each Teaching Round after coreference resolution. GPT-2 context was reset at the beginning of every round and initialised with an unscored boundary token. Tokens from the two or three sentences within a round remained continuous, allowing a later sentence to use earlier sentences from the same round as context. Previous Teaching Rounds were not included in the probability calculation. The PDB of each Teaching Round was therefore independent of its position within the full lesson and of the learner condition subsequently applied to it.

PDB was calculated offline and frozen before student simulation. For every Teaching Round $R_i$, all learner conditions used the same $D_\theta(R_i)$; it was not recalculated after later processing stages retained or removed information:

\[
R_i \longrightarrow D_\theta(R_i).
\]

Because PDB is cumulative, it necessarily contains a text-length component. Across the 83 frozen Teaching Rounds, GPT-2 PDB and surface word count had a Spearman correlation of $\rho=0.727$. Longer rounds therefore tended to have higher cumulative demand, but the two measures were not equivalent: differences in conditional predictability produced different PDB values among rounds of similar length. Word count and PDB were consequently retained together to describe surface length and language-model-based cumulative processing demand, respectively.

\subsubsection{Estimator-Sensitivity Analysis}

Surprisal is not a textual property that exists independently of its probability model. Models may produce different estimates because they differ in training corpus, tokenisation, context representation, and architecture. To test whether the relative instructional-demand structure used in this study depended excessively on GPT-2, a heterogeneous estimator---a fixed-discount interpolated Kneser--Ney word trigram---was used for sensitivity analysis. The trigram model was trained on the Brown and Reuters corpora and, like GPT-2, reset its context at every Teaching-Round boundary.

Because GPT-2 operates on byte-pair-encoded tokens whereas the Kneser--Ney model operates on word tokens, their raw cumulative bit values are not directly comparable. The sensitivity analysis therefore primarily compared the percentile-rank ordering of the 83 Teaching Rounds under the two estimators and additionally compared estimator-specific Low, Middle, and High quartile groups. Complete results are reported in Appendix Table~\ref{tab:appendix-pdb-sensitivity}.

The maximum GPT-2 reproduction error for the frozen PDB values was 0.000349 bits, and the maximum range across ten repeated computations was 0 bits, indicating deterministic and highly reproducible implementation. The Teaching-Round orderings produced by GPT-2 and Kneser--Ney were strongly positively associated, with Spearman $\rho=0.864$ and a lesson-stratified bootstrap 95\% confidence interval of $[0.782,0.917]$; within-lesson correlations ranged from $0.782$ to $0.952$. No direct Low-to-High reversal occurred between the two estimators. However, Low-tail and High-tail Jaccard similarity was 0.615 and 0.448, respectively; Low/Middle/High agreement was 68.7\%, with Cohen's $\kappa=0.501$. The overall relative ordering was therefore robust, while exact quartile membership remained sensitive to estimator choice.

GPT-2 small was retained as the formal PDB estimator because it directly outputs autoregressive next-token probabilities, permits the model and tokenizer versions to be fixed locally, produces deterministic repeated scores, avoids traditional word-level out-of-vocabulary problems through byte-level tokenisation, and yields a Teaching-Round ordering that showed strong relative agreement with the heterogeneous Kneser--Ney estimator. This selection does not assume that GPT-2 is the uniquely correct language model or the closest model of human cognition. It treats GPT-2 as a transparent, reproducible measurement instrument that has undergone a cross-estimator sensitivity check. The final PDB value was stored in the \texttt{processing\_demand\_bits} field of each frozen Teaching-Round record and used as a shared, fixed material-level annotation across all subsequent experimental conditions.

\subsection{Question Construction}
\label{subsec:question-construction}

Each lesson was paired with seven short-answer assessment questions: six Independent Questions and one Integrative Question. The seven lessons therefore produced 49 frozen questions, comprising 42 Independent Questions and seven Integrative Questions. Every question was constructed directly from the frozen instructional materials, and its content, source mapping, and reference answer were reviewed and frozen before student simulation began.

An \textbf{Independent Question} assesses knowledge that can be localised to a single Teaching Round. Each Independent Question maps to exactly one \texttt{source\_round\_id}, although its answer evidence may come from one or more source sentences within that round. These questions primarily examine whether the learner retained and can express a local definition, factual set, comparison, causal mechanism, or conceptual relation. Because each question maps uniquely to one Teaching Round, it also inherits that round's frozen \texttt{processing\_demand\_bits} value, supporting the subsequent processing-demand analysis. An \textbf{Integrative Question} requires information from multiple Teaching Rounds to be combined and cannot be answered from a single local sentence. In the frozen question set, each lesson's Integrative Question draws on three or four Teaching Rounds and primarily assesses cross-round relations, contrasts, shared frameworks, or causal chains.

Question construction began with a review of the instructional content at Teaching-Round level. A round entered the Independent-Question candidate set only when it contained information from which a clear question, reference answer, and bounded set of judgeable scoring points could be constructed. The procedure did not require every Teaching Round to produce a question: some rounds primarily provided contextual continuity or contained content that could not be decomposed cleanly into assessment propositions. Question construction therefore prioritised content validity and scoring clarity rather than mechanical coverage of every round.

The frozen questions were assigned to five question types, summarised in Table~\ref{tab:question-type-distribution}.

\begin{table}[H]
\centering
\caption{Question types, assessment focus, and frozen question counts.}
\label{tab:question-type-distribution}
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.27\textwidth}>{\raggedright\arraybackslash}p{0.62\textwidth}>{\centering\arraybackslash}p{0.06\textwidth}@{}}
\toprule
\textbf{Question type} & \textbf{Assessment focus} & \textbf{Count} \\
\midrule
Identification and Listing & Identify components, categories, or examples explicitly listed in the source & 19 \\
Comparison and Contrast & Explain distinctions or correspondences between two or more concepts & 12 \\
Relationship Explanation & Explain an explicit relation among concepts, roles, stages, or outcomes & 9 \\
Cause or Mechanism Explanation & Explain a source-grounded cause, process, or mechanism & 5 \\
Definition and Description & Define or describe an explicitly introduced concept & 4 \\
\bottomrule
\end{tabular}
\end{table}

These question types were selected because their expected answers could be decomposed into a bounded set of semantically explicit propositions traceable to source evidence. Identification and Listing questions, for example, allow separately listed source items to become independently judgeable scoring points. Comparison and Contrast questions allow the defining features of each comparison target to be assessed separately. Relationship Explanation and Cause or Mechanism Explanation questions allow the required entities, direction of relation, and outcome to be evaluated explicitly. Questions did not require verbatim reproduction of the instructional text, but every correct answer had to be supported by a meaning-preserving paraphrase of the frozen source. Criterion construction and scoring are specified in Section 3.2.4.

Candidate questions were further reviewed for question validity, source grounding, and rubric clarity. Only questions whose content and scoring requirements passed this review entered the final frozen selection.

Question-to-source mappings use the identifier system defined in Section 3.2.1. Each question has a unique \texttt{question\_id} and records its \texttt{lesson\_id}, \texttt{question\_scope}, \texttt{question\_type}, \texttt{source\_round\_ids}, \texttt{source\_sentence\_ids}, and \texttt{reference\_answer}. Independent Questions additionally inherit the \texttt{processing\_demand\_bits} value and \texttt{demand\_group} of their unique source Teaching Round.

Independent Questions map to one Teaching Round and its specific source sentences; Integrative Questions list the multiple round and sentence identifiers required to answer them. This structure makes every question and its later scoring criteria traceable to explicit source evidence:
\par\smallskip
\noindent\makebox[\textwidth][c]{\(\text{Lesson}\rightarrow\text{Teaching Round}\rightarrow\text{Source Sentence}\rightarrow\text{Question}\rightarrow\text{Checklist Criterion}.\)}
\par\smallskip
Question wording, source mappings, reference answers, and checklist criteria remained frozen during the formal experiments, ensuring that every experimental condition used the same assessment content and scoring basis.

\subsection{Scoring Rubric and Atomic Checklist Criteria}

Following the rubric-based and structured criterion-level evaluation principles reviewed in Section 2.6, each assessment question was represented using a frozen set of independently judgeable, source-grounded criteria rather than a single holistic scoring rubric. The study integrates these principles into the frozen criterion-level scoring pipeline shown in Figure~\ref{fig:criterion-scoring-pipeline}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,keepaspectratio]{Figures/Chapter3/export/ch03_criterion_scoring_pipeline.pdf}
    \caption{Frozen criterion-level scoring and validation pipeline.}
    \label{fig:criterion-scoring-pipeline}
\end{figure}

In this study, \textit{atomic} is an operational term: each criterion represents one instructional proposition that can be judged independently, rather than a uniquely indivisible linguistic fact. Criteria within the same question were designed to minimise semantic overlap while collectively covering the knowledge required by the reference answer. Each criterion was linked to supporting evidence in the frozen instructional materials, ensuring that the scoring checklist did not introduce requirements beyond the taught source content.

\subsubsection{Rubric Construction and Deterministic Score Calculation}
\label{subsec:rubric-construction}

The scoring rubric and checklist were constructed, reviewed, and frozen before formal student-answer generation and scoring. The LLM Judge cannot generate or modify scoring requirements during evaluation. Each assessment question is stored as a structured record containing a unique question identifier, question scope, question type, question text, reference answer, source Teaching Round, source sentence identifiers, and a checklist of atomic criteria. Each criterion additionally contains a unique \texttt{criterion\_id}, the proposition to be expressed, source sentence identifiers, and the original evidence span supporting that proposition. An independent question corresponds to one frozen Teaching Round, whereas an integrative question requires knowledge from multiple Teaching Rounds. Questions from all seven lessons use the same data structure, and the consistency among questions, reference answers, checklists, source identifiers, and evidence spans was checked before freezing.

During scoring, the LLM Judge receives only the following information:

\[
\left\{
\begin{array}{l}
\text{question ID},\\
\text{question},\\
\text{reference answer},\\
\text{checklist criteria},\\
\text{source-evidence spans},\\
\text{student answer}
\end{array}
\right\}.
\]

The Judge payload excludes learner identity, simulation condition, lesson label, demand group, Processing Demand Bits, memory state, Attention state, WM capacity, and all other experimental metadata. These local metadata are reattached to the scoring output only after the Judge response passes structural validation. This blind evaluation design prevents the Judge from inferring the score that a simulated learner should receive from the expected behaviour of a condition.

The Judge does not directly generate a numerical score. It independently assigns \texttt{Correct}, \texttt{Absent}, or \texttt{Contradicted} to each criterion. For \texttt{Correct} and \texttt{Contradicted}, it must also copy the shortest continuous span from the student answer that supports the judgement; for \texttt{Absent}, the evidence field must be null. The output contract prohibits the Judge from adding, deleting, merging, splitting, or redefining criteria. The Judge is also instructed to disregard answer length, writing style, fluency, grammar, vocabulary, confidence, formatting, and repetition, and to evaluate only the semantic propositions actually asserted by the student.

After the returned criterion identifiers and evidence spans pass local programmatic validation, Python calculates the question score deterministically. For question \(q\), containing \(K_q\) criteria,

\[
\operatorname{Score}_q
=
10
\times
\frac{
\sum_{k=1}^{K_q}
\mathbb{I}\!\left(y_{qk}=\mathrm{Correct}\right)
}{K_q},
\]

where \(y_{qk}\) is the label assigned to criterion \(k\), and \(\mathbb{I}(\cdot)\) is the indicator function. Both \texttt{Absent} and \texttt{Contradicted} contribute zero credit, but their labels are retained for error-type analysis. The procedure produces a standardised question score from 0 to 10 while preserving information about the specific knowledge components that the student retained or omitted.

\subsubsection{Criterion-Level LLM-as-a-Judge Scoring and Validation}
\label{subsec:judge-validation}

Before formal scoring, the study compared two candidate Judge models: \texttt{qwen3.7-max} and \texttt{gpt-5.6-sol}. Both models received the same frozen Judge prompt, assessment question, reference answer, atomic checklist criteria, and student answer, and returned only a \texttt{Correct}, \texttt{Absent}, or \texttt{Contradicted} label for each criterion. The validation dataset comprised the same 49 Pre-Knowledge Baseline answers, including 42 Independent Questions, seven Integrative Questions, and 137 atomic criteria. Each model independently scored all answers three times, producing 147 question-level calls per model and 294 calls in total.

Model selection was first governed by seven repeatability criteria specified before the results were generated: output schema, criterion-set, and evidence-span validity had to equal 1.00; Criterion-Label Unanimity had to be at least 0.95; Pairwise Criterion Agreement had to be at least 0.97; Exact Question-Score Invariance had to be at least 0.90; absolute-agreement $\operatorname{ICC}(A,1)$ had to be at least 0.95; the maximum difference between replicate overall mean scores could not exceed 0.10 points; and the minimum lesson-level Criterion-Label Unanimity had to be at least 0.90. These thresholds assessed only the internal repeatability of each candidate model.

As shown in Table~\ref{tab:judge-repeatability-thresholds}, Qwen achieved 1.000 for Criterion-Label Unanimity, Exact Question-Score Invariance, and $\operatorname{ICC}(A,1)$ across the three scoring repetitions. Its Maximum Replicate-Mean Difference was zero, and it passed all seven specified criteria. GPT achieved 0.9781, 0.9388, and 0.9700, respectively, and passed six criteria, but its Maximum Replicate-Mean Difference of 0.2041 exceeded the specified 0.10-point upper bound. GPT's three overall means were 7.925, 8.129, and 7.925, indicating that its residual variation was concentrated in a small number of criterion decisions. This instability was more pronounced for Integrative Questions, where Criterion-Label Unanimity, Exact Question-Score Invariance, and $\operatorname{ICC}(A,1)$ decreased to 0.9524, 0.8571, and 0.9231, respectively; Qwen remained fully invariant within this scope.

\begin{table}[H]
\centering
\caption{Three-run stability and prespecified-threshold evaluation of the candidate Judge models.}
\label{tab:judge-repeatability-thresholds}
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}>{\raggedright\arraybackslash}p{0.38\textwidth}rrrr@{}}
\toprule
\textbf{Stability metric} & \textbf{Criterion} & \textbf{Qwen3.7-max} & \textbf{GPT-5.6-sol} & \textbf{Qwen--GPT} \\
\midrule
Schema, criterion-set, and evidence validity & $=1.0000$ & 1.0000 & 1.0000 & 0.0000 \\
Overall criterion-label unanimity & $\geq0.9500$ & 1.0000 & 0.9781 & +0.0219 \\
Pairwise criterion agreement & $\geq0.9700$ & 1.0000 & 0.9854 & +0.0146 \\
Exact question-score invariance & $\geq0.9000$ & 1.0000 & 0.9388 & +0.0612 \\
$\operatorname{ICC}(A,1)$ & $\geq0.9500$ & 1.0000 & 0.9700 & +0.0300 \\
Maximum replicate-mean difference & $\leq0.1000$ & 0.0000 & 0.2041 & $-0.2041$ \\
Minimum lesson-level criterion unanimity & $\geq0.9000$ & 1.0000 & 0.9474 & +0.0526 \\
Prespecified criteria passed & -- & 7/7 & 6/7 & -- \\
\bottomrule
\end{tabular*}
\end{table}

To test whether the frozen checklist rubric could be applied consistently by different scorers, the study also conducted validation with two human raters. Human Judge 1 held a bachelor's degree in economics, while Human Judge 2 was a master's student with a computer-science background. Neither rater had participated in constructing the questions, reference answers, or checklist criteria. Both independently applied the same frozen scoring sheet under the same written scoring protocol and operational guidance. For every criterion, each rater viewed the question, criterion description, source evidence, and student answer, and selected \texttt{Correct}, \texttt{Absent}, or \texttt{Contradicted}. During their initial ratings, the two raters did not discuss the cases and could not see either the other rater's labels or the candidate LLM Judges' labels. They did not calculate question scores manually; after criterion labelling, the program aggregated criterion credits and normalised question scores using the rule defined in Section 3.2.4.1.

The two human raters agreed on 132 of the 137 criteria. As shown in Table~\ref{tab:human-model-scoring-agreement}, their criterion-level agreement was 96.35\%, Cohen's $\kappa$ was 0.795, Exact Question-Score Agreement was 89.80\%, and Question-Score MAE was 0.374. Human Judges 1 and 2 produced overall mean scores of 9.014 and 9.116, respectively. The five disagreements occurred in five different questions and all concerned a \texttt{Correct}--\texttt{Absent} boundary; none involved \texttt{Contradicted}.

After the initial independent ratings, the study conducted a structured audit of the five disagreements. The audit compared the relevant question, criterion, source evidence, student answer, and each rater's recorded evidence and justification. Its purpose was to characterise the disagreement rather than to force a consensus label or overwrite either original judgement. The five cases concerned semantic-entailment boundaries: whether all required components had to appear, whether asset allocation sufficiently expressed diversification, whether an implicit contrast expressed an explicit negation, whether specific job titles satisfied a superordinate role category, and whether distributed technical evidence jointly expressed a complete relation. These cases therefore reflected different boundaries for deciding when related information became sufficient to satisfy a criterion, rather than different understandings of the instructional facts. Atomic checklists substantially narrowed the scoring space but did not eliminate uncertainty in natural-language entailment. The complete audit is reported in Appendix Table~\ref{tab:appendix-human-disagreement-audit}.

The human ratings also revealed substantial upper-end concentration in the validation sample. Human Judge 1 classified 123/137 criteria as \texttt{Correct} and awarded full credit to 37/49 questions; Human Judge 2 classified 124/137 criteria as \texttt{Correct} and awarded full credit to 38/49 questions. Their full-score rates were therefore 75.51\% and 77.55\%. Because high raw agreement partly occurred under an imbalanced label distribution and near-ceiling performance, the analysis also reports chance-corrected Cohen's $\kappa$, Exact Question-Score Agreement, and MAE rather than treating raw agreement alone as sufficient evidence of scoring validity.

No human rater was designated as a unique gold standard. Model--human alignment was therefore calculated separately using each human rater as the reference. Qwen's criterion agreement with the two raters ranged from 84.67\% to 85.40\%, Cohen's $\kappa$ from 0.457 to 0.475, Exact Question-Score Agreement from 69.39\% to 73.47\%, and Question-Score MAE from 1.429 to 1.531. GPT's corresponding ranges were 85.40\%--87.59\%, 0.452--0.541, 71.43\%--73.47\%, and 1.224--1.327. These results do not show a clear model--human alignment advantage for Qwen; its principal advantage was its higher repeated-measure stability.

\begin{table}[H]
\centering
\caption{Human--human and model--human scoring agreement.}
\label{tab:human-model-scoring-agreement}
\footnotesize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.29\textwidth}>{\centering\arraybackslash}p{0.16\textwidth}>{\centering\arraybackslash}p{0.13\textwidth}>{\centering\arraybackslash}p{0.18\textwidth}>{\centering\arraybackslash}p{0.10\textwidth}@{}}
\toprule
\textbf{Comparison} & \textbf{Criterion agreement} & \textbf{Cohen's $\kappa$} & \textbf{Exact score agreement} & \textbf{Score MAE} \\
\midrule
Human Judge 1 vs Human Judge 2 & 0.9635 & 0.7946 & 0.8980 & 0.3741 \\
Qwen3.7-max vs Human Judge 1 & 0.8467 & 0.4569 & 0.6939 & 1.5306 \\
Qwen3.7-max vs Human Judge 2 & 0.8540 & 0.4753 & 0.7347 & 1.4286 \\
GPT-5.6-sol vs Human Judge 1 & 0.8759 & 0.5414 & 0.7347 & 1.2245 \\
GPT-5.6-sol vs Human Judge 2 & 0.8540 & 0.4520 & 0.7143 & 1.3265 \\
\bottomrule
\end{tabular}

\vspace{2pt}
\parbox{\textwidth}{\footnotesize\textit{Note.} Human--human results use all 137 criteria and 49 questions. Model results use criterion-wise modal labels across three repetitions. Human Judges 1 and 2 were treated as independent references; neither was designated as a definitive gold standard.}
\end{table}

Model--human comparisons used criterion-wise modal labels across the three repetitions. Under this aggregation, Qwen and GPT produced overall mean scores of 7.823 and 7.925, respectively. Qwen and GPT classified 106/137 and 108/137 criteria as \texttt{Correct}, and both awarded full credit to 29/49 questions, fewer than the 37 and 38 full-score questions awarded by the two human raters. The disagreement therefore involved not only local criterion boundaries but also a systematic score-level stringency difference. This difference is treated as a limitation of automated scoring rather than as evidence that the model is closer to or better than human judgement.

Qwen was retained as the operational Judge because it was the only candidate to pass all seven prespecified repeatability criteria. Human alignment was not introduced retrospectively as a model-selection threshold, and the results do not support describing Qwen as closer to human judgement than GPT. More precisely, Qwen applied the frozen source-grounded checklist more consistently across repeated calls, but its scoring boundary was not equivalent to either human rater. Subsequent results are therefore interpreted as Qwen's stable application of the frozen scoring protocol to the current materials, questions, and answer distribution, rather than as evidence that its scores are generally interchangeable with human ratings. The complete operational prompt is provided in Appendix Figure~\ref{fig:appendix-judge-prompt}.
\section{Learner Representation: Persona Prompting versus CPB}

Figure~\ref{fig:persona-cpb-learning-path-comparison} presents the two learner-representation pipelines compared in this study: Persona Prompting and the Cognitive-Process-Based (CPB) framework. Both approaches use matched Teaching Rounds and the same assessment and scoring procedures, but they differ systematically in how learner characteristics are represented, how the learning process is structured, what knowledge state is retained after teaching, and which information is available during answer generation.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,keepaspectratio]{Figures/Chapter3/export/ch03_persona_cpb_learning_path_comparison.pdf}
    \caption{Representation-level comparison of Persona Prompting and the CPB learner pipeline.}
    \label{fig:persona-cpb-learning-path-comparison}
\end{figure}

In Persona Prompting, explicit persona descriptions primarily represent the target learner characteristics. Instructional information remains available in the accumulated full teaching context and is used jointly with the persona instructions when an assessment answer is generated. CPB instead operationalises the selected ADHD-related characteristics as functional constraints acting during learning. Instructional information passes through explicit processing stages to form an Explicit Long-Term Memory (LTM), and assessment answers are subsequently restricted to that knowledge state.

The two approaches therefore instantiate different learner representation pipelines. Persona Prompting represents learner characteristics as descriptive instructions that condition generated behaviour directly, while retaining the complete accumulated teaching context as the answer-stage information source. CPB locates the selected cognitive characteristics within the learning process itself: functional constraints alter information availability, the resulting input is encoded into an explicit knowledge state, and that state constrains subsequent response generation.

Beneath this overall architectural distinction, the first difference concerns how instructional information is retained and used after exposure. As illustrated in Figure~\ref{fig:persona-cpb-answer-information-source}, Persona Prompting uses the complete \texttt{[CLASSROOM\_EXPERIENCE]} as the knowledge source for later answers. As Teaching Rounds proceed, the earlier teaching material and corresponding student turns remain accumulated in the conversation context and can be accessed again during assessment. The teaching experience is therefore not transformed into a separate learner-specific knowledge representation; instead, the complete interaction history carries the previously presented instructional information.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,keepaspectratio]{Figures/Chapter3/export/ch03_persona_cpb_answer_information_source_comparison.png}
    \caption{Answer-stage information available to Persona Prompting and CPB.}
    \label{fig:persona-cpb-answer-information-source}
\end{figure}

CPB, by contrast, produces an explicit \texttt{[LEARNED\_MEMORY]} during the Teaching Phase. In each Teaching Round, only information that remains available after Attention and Working-Memory processing reaches the Knowledge Encoder and is transformed into an Explicit-LTM entry. Explicit LTM is therefore not a complete copy of the classroom record, but an explicit knowledge representation produced from the outcome of the preceding processing stages. The example in Figure~\ref{fig:persona-cpb-answer-information-source} illustrates this distinction: the Persona branch preserves the original Teaching Rounds and classroom interaction, whereas the CPB branch stores knowledge-oriented memory entries produced through processing and encoding.

Consequently, the two representations do not access the same type of information during assessment. A persona-prompted learner can answer from the complete accumulated teaching history. A CPB learner can access only information that entered Explicit LTM; instructional content made unavailable during an earlier Attention or Working-Memory stage does not reappear merely because the learner has entered the Assessment Phase. From a knowledge-representation perspective, the two approaches can therefore be characterised as \textbf{full-context-based answering} and \textbf{explicit-memory-based answering}. The former preserves continuing access to the full classroom history, whereas the latter connects teaching to assessment through an explicit memory state produced by the learning pipeline. This distinction describes how the two simulation architectures operationalise learner knowledge state; it does not presuppose that either approach is inherently more accurate or psychologically faithful.

The answer-stage prompt structures shown in Figure~\ref{fig:persona-cpb-answering-prompts} further reflect these different forms of learner representation. The persona-prompted learner receives an explicit ADHD-related \texttt{[COGNITIVE\_PROMPT]} in its system prompt. This component was adapted from the ADHD persona design of Gonnermann-Müller et al. \cite{gonnermann-muller-etal-2026-adhd-persona}. Its user message contains the accumulated \texttt{[CLASSROOM\_EXPERIENCE]}. The current \texttt{[ASSESSMENT\_QUESTION]} is added separately, so answer generation is conditioned jointly by the persona instructions and the complete teaching context.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,keepaspectratio]{Figures/Chapter3/export/ch03_prompt_cpb_answering_prompt_comparison.pdf}
    \caption{Answer-stage prompt composition in Persona Prompting and CPB.}
    \label{fig:persona-cpb-answering-prompts}
\end{figure}

CPB does not provide an ADHD identity or ADHD-related role-playing instruction at the answer stage. Its system message instead contains a \texttt{[MEMORY\_RESTRICTION]} requiring the model to answer the current \texttt{[QUESTION]} using only the process-derived \texttt{[LEARNED\_MEMORY]}. The ADHD-related Attention and Working-Memory characteristics are thus represented primarily by learning-stage mechanisms rather than being restated as response-level instructions. Complete system-prompt examples for the two representations are provided in Appendix Figures~\ref{fig:appendix-prompt-adhd-example} and~\ref{fig:appendix-cpb-prompt-example}.

The \texttt{[LANGUAGE\_ABILITY]} and \texttt{[PERSONALITY\_DESCRIPTION]} components shown in Figure~\ref{fig:persona-cpb-answering-prompts} are enabled only in Study 3; they are absent from Studies 1 and 2. These components were adapted from \textit{Personality-aware Student Simulation for Conversational Intelligent Tutoring Systems} \cite{liu-etal-2024-personality}. They are added as response-stage attributes to both learner representations in order to examine how additional profile dimensions affect assessment behaviour and the previously established CPB patterns. Both branches otherwise share the core student instruction and receive the same assessment task.

The comparison can be summarised at three related levels: how learner characteristics are represented, what knowledge representation is retained after learning, and how the retained information participates in answer generation. Table~\ref{tab:persona-cpb-architectural-comparison} summarises these distinctions.

\begin{table}[H]
\centering
\caption{Core architectural differences between Persona Prompting and CPB.}
\label{tab:persona-cpb-architectural-comparison}
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.2}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}>{\raggedright\arraybackslash}p{0.21\textwidth}>{\raggedright\arraybackslash}p{0.35\textwidth}>{\raggedright\arraybackslash}p{0.35\textwidth}@{}}
\toprule
\textbf{Comparison level} & \textbf{Persona Prompting} & \textbf{CPB} \\
\midrule
Learner representation & Target characteristics are described through an explicit \texttt{[COGNITIVE\_PROMPT]}. & Attention- and Working-Memory-related characteristics are operationalised as functional processing constraints. \\
Knowledge representation & The complete accumulated classroom experience remains available. & Processing and Knowledge Encoding produce an Explicit LTM. \\
Answer generation & Persona instructions and the full teaching context jointly condition the answer. & No ADHD persona is provided at answer time; answering is restricted to the learned memory. \\
\bottomrule
\end{tabular*}
\end{table}

Together, these distinctions define the two learner-representation architectures evaluated in the subsequent experiments: a description-conditioned, full-context approach and a process-constrained, explicit-memory-based approach. Section~\ref{sec:cpb-mechanisms} next specifies the implementation of the CPB learning mechanisms in detail.

\section{Cognitive-Process-Based Mechanisms}
\label{sec:cpb-mechanisms}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,keepaspectratio]{Figures/Chapter3/export/ch03_cpb_learning_information_flow.pdf}
    \caption{CPB learning-phase information flow from the frozen Teaching Round to Explicit LTM.}
    \label{fig:cpb-learning-information-flow}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.88\textwidth,keepaspectratio]{Figures/Chapter3/export/ch03_cpb_assessment_phase_flow.pdf}
    \caption{CPB assessment-phase information flow under Explicit-LTM-constrained answering.}
    \label{fig:cpb-assessment-phase-flow}
\end{figure}

Throughout the CPB pipeline, \(r\) indexes the frozen Teaching Round and \(c\) indexes the experimental condition. \(S_r\) denotes the condition-invariant instructional Source; \(P_{r,c}\) denotes the post-Attention information state; \(A_{r,c}\) denotes the Available Input remaining after Working-Memory Processing; and \(L_{r,c}\) denotes the Explicit LTM generated by the shared Knowledge Encoder.

As illustrated in Figures~\ref{fig:cpb-learning-information-flow} and~\ref{fig:cpb-assessment-phase-flow}, CPB represents learning as a sequence of explicit information-state transformations. For each Teaching Round, the frozen instructional Source \(S_r\) first enters the Attention Filter, producing the post-Attention input \(P_{r,c}\). This information then enters Working-Memory Processing, where instructional processing demand and the condition-specific WM capacity further constrain it to form the Available Input \(A_{r,c}\). The Knowledge Encoder shared across all experimental conditions transforms this input into Explicit LTM \(L_{r,c}\). During the Assessment Phase, the CPB learner no longer accesses the original instructional materials; it answers the current assessment question using only the accumulated Explicit LTM. The CPB learning process can therefore be expressed as the continuous transformation \(S_r \rightarrow P_{r,c} \rightarrow A_{r,c} \rightarrow L_{r,c}\). Attention and Working Memory constitute the principal ADHD-related experimental constraints, whereas Knowledge Encoding remains a shared downstream stage.

\subsection{Attention Filter}
\label{subsec:attention-filter}

\subsubsection{Attention Filter Implementation}
\label{subsubsec:attention-filter-implementation}

As reviewed in Section~\ref{sec:adhd-learning-processes}, ADHD-related attentional differences can include increased susceptibility to task-irrelevant distraction, but the effects of distraction vary across learners and task conditions. The present study therefore models \textbf{susceptibility to controlled distraction} as a bounded functional abstraction rather than attempting to reproduce human attentional control as a whole. The implementation follows a simple principle: a distractor event is inserted immediately before a predefined assessment-relevant target sentence; when that distractor triggers the Attention Filter, the target sentence becomes unavailable for subsequent processing. Distraction is thereby represented through its downstream effect on information availability rather than through a persona-level instruction such as ``be easily distracted.''

\begin{figure}[H]
    \centering
    \includegraphics[width=0.62\textwidth,trim=720bp 80bp 720bp 80bp,clip,keepaspectratio]{Figures/Chapter3/export/ch03_attention_filter_decision_flow.png}
    \caption{Attention Filter control flow.}
    \label{fig:attention-filter-decision-flow}
\end{figure}

Figure~\ref{fig:attention-filter-decision-flow} summarises the control flow. For each Teaching Round, the mechanism first checks whether a frozen distractor assignment is present. If present, triggering is determined by the condition-specific Attention probability \(p^A_c\). A triggered distractor makes its predefined target sentence unavailable; otherwise, the instructional content is preserved. All branches produce the post-Attention state \(P_{r,c}\).

The affected instructional information is fixed before student simulation. Each Independent Question is already mapped to its supporting source sentence or sentences; one of these sentences is selected as the Attention target, and a bracketed distractor event is inserted immediately before it in the distracted version of the teaching material. Each assignment records \texttt{linked\_question\_id}, \texttt{distractor\_event}, and \texttt{target\_sentence\_id}. Integrative Questions do not receive additional independent distractor assignments. For example:

\begin{quote}
\footnotesize\ttfamily
Finance examines how money is obtained, managed, moved, and used.

[A seagull flies past and lands on the windowsill]

Finance helps individuals and organizations understand sources and uses of cash and make decisions when outcomes are uncertain.
\end{quote}

Here, the sentence following the bracketed distractor is the frozen target. The associated assessment question is not provided to the student model during learning, and the Attention mechanism does not dynamically select information based on the later question. The term \textit{question-targeted} therefore refers only to the preregistered alignment between the distractor, the affected source evidence, and the subsequent assessment item.

For an eligible distractor in Teaching Round \(r\), repeated run \(j\), and condition \(c\), the trigger decision is sampled as

\[
Z^A_{r,c,j}\sim \operatorname{Bernoulli}(p^A_c),
\]

where \(Z^A_{r,c,j}=1\) indicates that the distractor triggers the Attention Filter and its predefined target sentence becomes unavailable, while \(Z^A_{r,c,j}=0\) leaves the target sentence available. The boundary values \(p^A_c=0\) and \(p^A_c=1\) represent deterministic no-trigger and always-trigger conditions. Run-specific random seeds, draws, trigger decisions, and affected target identifiers are retained so that each stochastic Attention trajectory can be reconstructed.

The resulting transformation can be written as

\[
X_r \xrightarrow{\mathrm{Attention}} P_{r,c},
\]

where \(X_r\) is the presented classroom input, including any assigned distractor, and \(P_{r,c}\) is the instructional information that remains available for subsequent Working-Memory Processing. The Attention Filter therefore affects only downstream information availability: it does not directly modify Explicit LTM, generate assessment answers, or determine which information is relevant at answer time.

Treating a triggered target sentence as unavailable is a study-specific computational abstraction chosen for controllability and traceability, rather than a claim that human attentional disruption literally deletes information. The hard-deletion choice and its operationalisation sensitivity are examined in the next section, while study-specific values of \(p^A_c\) are defined in Sections~\ref{subsubsec:study1-conditions} and~\ref{subsubsec:study2-conditions}.

\subsubsection{Operationalisation Sensitivity Check}
\label{sec:attention-operationalisation-sensitivity}

Direct deletion provides a simple and controllable way to represent instructional information as unavailable, and similar input-removal operations have been used in NLP perturbation studies \cite{feng-etal-2018-pathologies}. However, prior work also shows that different representations of missing information, such as deletion and masking, can produce different downstream model behaviour \cite{lewis-etal-2020-bart}. Therefore, the choice of unavailable-content representation cannot be assumed to be behaviourally neutral.

To test whether the observed CPB effects depend on this implementation choice, the present study compares two representation policies: \textbf{hard deletion}, which removes the target sentence entirely, and \textbf{explicit masking}, which removes its original semantic content while retaining a visible missingness cue. This sensitivity check is used only to assess whether the main Attention-related results are robust to the surface representation of unavailable information.

\[
g_{\mathrm{delete}}(s)=\emptyset,
\]

\[
g_{\mathrm{mask}}(s)
=
\texttt{[CONTENT UNAVAILABLE: ...]}.
\]

The two implementations therefore have different informational implications. Hard deletion prevents the target instructional content from entering subsequent processing, whereas explicit masking removes the original semantics but additionally provides the model with a metacue indicating that information is missing at that location. Character-length matching guarantees only the same Python character count and does not imply an equivalent token count.

To test whether this representation choice affects downstream processing, the study conducted an independent sensitivity experiment using two frozen lessons, L01 and L02. The experiment included three mechanism conditions in which information could become unavailable:

\begin{enumerate}
    \item Attention ON / WM OFF;
    \item Attention OFF / WM ON; and
    \item Attention ON / WM ON.
\end{enumerate}

In this experiment, Attention ON means that the Attention Filter is triggered with 100\% probability. Attention OFF / WM OFF was excluded because this condition contains no sentence marked as unavailable and therefore produces no actual difference between the two representation policies.

Each mechanism condition was run three times under each of the two representation policies, hard deletion and explicit masking. Each lesson contained seven frozen assessment questions, including six Independent Questions and one cross-round Integrative Question, producing 252 answers in total. Across all paired-round checks, the Attention trigger, Attention-unavailable sentence identifiers, and the other recorded CPB mechanism variables were identical between policies in 100\% of cases. The primary comparison was defined as

\[
\Delta \mathrm{Score}
=
\mathrm{Score}_{\mathrm{mask}}
-
\mathrm{Score}_{\mathrm{delete}}.
\]

To determine whether masking could be treated as a practically interchangeable implementation of deletion, a practical-equivalence margin of \(\pm 0.5\) points was specified in advance. A cluster bootstrap over the 14 frozen question clusters was used to calculate a 95\% interval. Practical equivalence was considered supported only if

\[
\mathrm{CI}_{95\%}
\subseteq
[-0.5,+0.5].
\]

The sensitivity analysis showed that most matched outputs were unchanged across the two implementations, but practical equivalence was not established within the prespecified margin. The overall mask-minus-deletion score difference was

\[
\Delta=-0.370,
\]

with a 95\% interval of

\[
[-1.005,0.106].
\]

Although the interval crossed zero and therefore did not support a stable directional difference, it was not fully contained within the \(\pm 0.5\) practical-equivalence margin. At the same time, 91.27\% of paired question scores were identical, while the small number of discrepant cases produced comparatively large downstream score shifts and were concentrated mainly in conditions involving the Attention manipulation.

Hard deletion was ultimately selected not because the sensitivity experiment demonstrated that it was psychologically closer to human ADHD-related attention than masking, but because of the combined considerations of \textit{construct correspondence}, \textit{minimal intervention}, and the sensitivity evidence. The Attention Filter is intended to represent a state in which target instructional information is no longer available to downstream processing. Hard deletion implements this change directly, whereas explicit masking removes the target semantics while introducing an additional explicit missingness cue. Hard deletion was therefore frozen as the formal unavailable-content policy.

This choice does not imply that hard deletion has been validated as the psychological mechanism underlying human ADHD-related attentional disruption. Direct deletion reduces both target semantics and the length of the downstream instructional input and therefore remains a study-specific computational operationalisation. The present sensitivity evidence supports it only as the more direct and traceable formal implementation used in this study, with fewer additional representational signals.

\subsection{Working-Memory Processing Capacity}

\subsubsection{Working-Memory Mechanism Implementation}
\label{sec:wm-mechanism-implementation}

As reviewed in Section~\ref{sec:adhd-learning-processes}, Working Memory (WM) is a limited processing system involved in maintaining and manipulating task-relevant information, and ADHD-related WM differences may become more consequential as processing demands increase. The present study therefore models \textbf{sensitivity to instructional processing demand} as a bounded functional abstraction rather than attempting to reproduce human Working Memory as a whole. Within CPB, the WM mechanism operates after Attention: when the frozen processing demand of a Teaching Round exceeds the capacity threshold assigned to the current condition, the earliest instructional sentence still available after Attention is made unavailable before Knowledge Encoding.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.72\textwidth,keepaspectratio]{Figures/Chapter3/export/ch03_working_memory_mechanism_flow.png}
    \caption{Working-Memory mechanism flow. Frozen source-round demand $D(R_r)$ is compared with the condition-specific threshold $C^{WM}_c$. When demand exceeds capacity, the earliest sentence remaining in $P_{r,c}$ is removed before the resulting Available Input $A_{r,c}$ proceeds to Knowledge Encoding.}
    \label{fig:working-memory-mechanism-flow}
\end{figure}

For Teaching Round $R_r$, the WM trigger is determined by

\[
\operatorname{WMOverflow}(R_r,c)
=
\mathbf{1}\left[D(R_r)>C^{WM}_c\right],
\]

where $D(R_r)$ is the Processing Demand Bits (PDB) value calculated and frozen for the original source round, and $C^{WM}_c$ is the capacity threshold assigned to condition $c$. The same $D(R_r)$ is used across all learner conditions and is not recalculated after Attention, ensuring that instructional demand remains a condition-independent property of the source material. Unlike the probabilistic Attention trigger, the WM rule is deterministic: for a fixed Teaching Round and capacity threshold, the trigger decision is always the same.

When overflow occurs, the mechanism acts only on instructional information that remains available in the post-Attention state $P_{r,c}$. If at least one sentence remains, the earliest available sentence in the original teaching order is selected for removal:

\[
R^{WM}_{r,c}
=
\{\operatorname{First}(P_{r,c})\},
\]

and the Available Input passed to Knowledge Encoding becomes

\[
A_{r,c}
=
P_{r,c}\setminus R^{WM}_{r,c}.
\]

If WM does not trigger, $A_{r,c}=P_{r,c}$. Each Teaching Round permits at most one WM-related sentence removal, and information already made unavailable by Attention cannot be removed again. The mechanism is therefore implemented as a \textbf{deterministic within-round FIFO-style information-restriction rule}.

The overall WM transformation can thus be represented as

\[
P_{r,c}
\xrightarrow{\mathrm{Working\ Memory}}
A_{r,c},
\]

where Attention determines what information remains available before WM processing, and WM further constrains how much of that remaining information can proceed to the shared Knowledge Encoder under the current demand--capacity condition. The capacity threshold $C^{WM}_c$ uses the same numerical scale as PDB but does not represent a literal number of human memory slots, sentences, or tokens. It is a study-specific control parameter for manipulating processing-constraint strength. The WM mechanism does not attempt to estimate the physiological working-memory capacity of real students. Study-specific thresholds and their experimental roles are reported in Sections~\ref{subsubsec:study1-conditions} and~\ref{subsubsec:study2-conditions}.

\subsection{Knowledge Encoding}

\subsubsection{Knowledge Encoding Implementation}
\label{sec:knowledge-encoding-implementation}

As reviewed in Section~\ref{sec:process-state-learner-modelling}, information that remains available after Attention and Working-Memory processing is conceptually distinct from a persistent learner knowledge state. CPB therefore includes a separate Knowledge-Encoding stage between Available Input and Explicit LTM. Encoding is not manipulated as an additional ADHD-specific mechanism; instead, all learner conditions use the same Encoder procedure so that differences in downstream knowledge state can be interpreted relative to the information that actually reaches Encoding. For Teaching Round $r$ and condition $c$, the transformation is represented as

\[
A_{r,c}
\xrightarrow{\mathrm{Encoding}}
L_{r,c},
\]

where $A_{r,c}$ is the Available Input remaining after Attention and Working-Memory processing and $L_{r,c}$ is the resulting Explicit LTM entry.

Within CPB, Knowledge Encoding uses each Teaching Round as its basic unit of processing and is executed after Attention and Working-Memory processing. For Teaching Round $r$ under condition $c$, the Encoder receives only the Available Input $A_{r,c}$ produced by the preceding mechanisms, rather than the original instructional source or the complete Teaching Round. Information already marked as unavailable by Attention or WM is not supplied to the Encoder again. The process is represented as

\[
L_{r,c}
=
\operatorname{Encode}(A_{r,c}),
\]

where $L_{r,c}$ is the round-level Explicit LTM entry formed after Encoding. The preceding mechanisms therefore determine \textbf{what information reaches Encoding}, while the Knowledge Encoder performs a representational transformation only on the information that actually reaches this stage.

Figure~\ref{fig:knowledge-encoding-prompt} presents the frozen Knowledge-Encoding prompt used in the formal experiments. The prompt requires the model to generate one brief long-term memory entry based only on the classroom information actually received, preserve the main concepts and their relationships in its own words, and avoid using outside knowledge or completing information absent from the input. Output is restricted to a single plain-text paragraph to reduce additional formatting differences in the subsequent Explicit LTM representation.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.88\textwidth,keepaspectratio]{Figures/Chapter3/export/ch03_knowledge_encoding_prompt_example.pdf}
    \caption{Frozen prompt template used by the shared Knowledge Encoder.}
    \label{fig:knowledge-encoding-prompt}
\end{figure}

Knowledge Encoding is not intended to copy the Available Input verbatim, but to transform it into a concise semantic knowledge representation. Accordingly, $A_{r,c}$ and $L_{r,c}$ are not assumed to be identical. The Encoder may paraphrase, compress, and reorganise the input, but the resulting memory entry should remain constrained by the information provided in $A_{r,c}$ and should not recover instructional content that has already become unavailable. Figure~\ref{fig:knowledge-encoding-example} presents an actual Encoding example: the Available Instructional Input $A_{r,c}$ entering the Encoder appears on the left, and the Encoded LTM Entry $L_{r,c}$ generated from the same content appears on the right. The example shows that the main concepts and their relationships can be retained after Encoding even though the wording and degree of information compression differ from the input text.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.96\textwidth,keepaspectratio]{Figures/Chapter3/export/ch03_knowledge_encoding_example.pdf}
    \caption{Example transformation from Available Instructional Input to an encoded LTM entry.}
    \label{fig:knowledge-encoding-example}
\end{figure}

The Encoding procedure is executed independently for each Teaching Round and produces at most one corresponding $L_{r,c}$. When no instructional information remains available for Encoding after Attention and WM processing, such that $A_{r,c}=\emptyset$, the system does not independently generate a new instructional memory from distractor content, prompt context, or the model's pretrained knowledge. This rule ensures that Explicit LTM originates only from instructional information that remains available after the preceding cognitive-processing stages and preserves a traceable relationship between Available Input and the encoded knowledge state.

Knowledge Encoding is not treated as an ADHD-specific mechanism in this study; instead, a fixed, shared Knowledge-Encoding procedure is used. All conditions use the same Encoder model, encoding prompt, input structure, and generation configuration. Attention, WM-constraint level, and other learner-condition metadata do not alter the Encoding instruction. This modelling decision does not constitute an empirical assumption that real ADHD and NT learners have completely identical encoding abilities.

\subsubsection{Pretrained-Knowledge and Memory-Constrained Answering}
\label{sec:pretrained-knowledge-memory-control}

In the formal experiments, the CPB Assessment Phase requires the simulated learner to answer questions using only the Explicit LTM formed during the Teaching Phase. However, because the large language model already contains pretrained knowledge, two alternative explanations must first be excluded. First, the student model may be able to answer most assessment questions using pretrained knowledge without any exposure to the course material, thereby compressing the observable space for the subsequent learning manipulations. Second, even when Explicit LTM is provided and the prompt requires the model to answer only from memory, the model may bypass this restriction and use its existing financial knowledge to complete the answer. To test these two prerequisites, the study conducted a \textbf{Question-Only Baseline} and a \textbf{Biology-Memory-Only Control}.

In the Question-Only Baseline, DeepSeek V4-Flash received no course material, teaching history, or Explicit LTM and directly answered the 49 financial questions in the formal assessment set. The generated answers were then scored by the same Qwen3.7-Max Judge used in the formal experiments. The mean score in this condition was

\[
\bar S_{\mathrm{Baseline}}=7.823.
\]

This result shows that DeepSeek V4-Flash already possesses some pretrained financial-domain knowledge, so the formal experiments cannot assume that assessment performance originates entirely from the Teaching Phase. At the same time, the baseline remained below the maximum score of 10, indicating that the question set retained observable learning headroom.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth,keepaspectratio]{Figures/Chapter3/export/ch03_memory_restriction_prompt_example.pdf}
    \caption{Memory Restriction prompt.}
    \label{fig:memory-restriction-prompt}
\end{figure}

To determine whether the \textbf{Memory Restriction} shown in Figure~\ref{fig:memory-restriction-prompt} actually constrained the model's information source during the Assessment Phase, the study constructed a Biology Explicit LTM that was unrelated to the financial course content but similar in format and word count, and used it to replace the Explicit LTM formed after learning the formal financial course. The Biology memory covered cells, genetics, physiology, evolution, and ecology and contained no finance, accounting, market, or investment knowledge. DeepSeek V4-Flash then answered the same 49 financial assessment questions under the same memory-constrained answering prompt.

The answering prompt explicitly required the model to answer only from the provided learned memory and prohibited the use of information not contained in that memory. All answers were evaluated by the same Qwen3.7-Max Judge against the same 137 frozen atomic criteria.

The results of the Biology-Memory-Only condition are reported in Table~\ref{tab:pretrained-memory-controls}.

\begin{table}[H]
\centering
\caption{Mean Checklist Score comparison between the Question-Only Baseline and Biology-Memory-Only Control.}
\label{tab:pretrained-memory-controls}
\small
\begin{tabular}{lr}
\toprule
\textbf{Condition} & \textbf{Mean Score} \\
\midrule
Question-Only Baseline & 7.823 \\
Biology Memory Only & 0.000 \\
Difference & $-7.823$ \\
\bottomrule
\end{tabular}
\end{table}

In the Biology-Memory-Only condition, all 49 assessment questions received a final score of zero. The Judge labelled all 137 rubric criteria as \texttt{Absent}, with \texttt{Correct}=0 and \texttt{Contradicted}=0, and the mean score was zero for every one of the seven lessons. This produced a clear contrast: without the memory restriction, the model could use pretrained knowledge to obtain a high score on the financial questions; when the supplied Explicit LTM contained no relevant financial knowledge and the model was required to answer only from that memory, its financial-question score fell to zero.

This result supports the \textbf{operational effectiveness of memory-access restriction in the current student-model setting}. More precisely, when the provided Explicit LTM lacked the information required to answer an assessment question, DeepSeek V4-Flash did not exhibit behaviour indicating that it used pretrained financial knowledge to complete the correct answer under the current answering prompt. The control therefore verifies that the model's compliance with the instruction to answer only from the supplied memory operated as expected.

On the basis of these two controls, the subsequent formal assessment procedure treats the frozen Explicit Knowledge State as the simulated learner's only experimental knowledge source. For condition $c$, the round-level LTM entries formed across Teaching Rounds are accumulated and frozen as

\[
\mathcal{L}_c=
\{L_{1,c},L_{2,c},\ldots,L_{R,c}\}.
\]

Each assessment question $Q_q$ independently generates an answer from the same frozen Explicit Knowledge State:

\[
\mathcal{L}_c + Q_q
\rightarrow
Y_{q,c}.
\]

The original instructional source, complete teaching history, and information previously marked as unavailable by Attention or Working Memory are not supplied to the answering model. Knowledge Encoding and question answering represent, respectively, the same simulated learner's knowledge-formation and knowledge-use stages. Because the corresponding encoding and memory-restriction prompts behaved stably with DeepSeek V4-Flash, the formal experiments use DeepSeek V4-Flash for both stages to avoid introducing additional cross-model differences.

Taken together, the Question-Only Baseline shows that the student model had not completely mastered the assessment set without teaching and that observable learning headroom remained. The Biology-Memory-Only Control shows that answer generation was effectively constrained by the supplied Explicit LTM under the current prompt and model configuration. These two controls jointly strengthen the attribution of subsequent performance differences to the manipulated learning processes and the Explicit Knowledge States they produced, rather than to unrestricted use of pretrained financial knowledge during the Assessment Phase.


\section{Research Design and Evaluation Strategy}

\subsection{Study 1---Mechanistic Validity of CPB}
\label{subsec:study1-design}

\subsubsection{Research Question and Subquestions}

Study 1 addresses RQ1 by examining whether the internal CPB processing pipeline produces correct, attributable, and traceable information-state changes under the predefined mechanisms. RQ1 is restated as follows:

\begin{quote}
\small
\textbf{RQ1---Mechanism:} Does the CPB processing pipeline produce intended and traceable transitions from instructional information, through attention, working-memory processing and encoding, to the downstream knowledge state?
\end{quote}

RQ1 concerns mechanism execution, stage-specific information change, and the propagation of those changes to downstream knowledge states. Final assessment performance alone cannot establish all three. RQ1 is therefore decomposed into three linked subquestions (SQs), progressing from low-level implementation fidelity to end-to-end propagation, as summarised in Table~\ref{tab:study1-subquestions}.

\begin{table}[H]
\centering
\caption{Subquestions and validation focus for Study 1.}
\label{tab:study1-subquestions}
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{p{0.15\textwidth} p{0.26\textwidth} p{0.51\textwidth}}
\toprule
\textbf{Subquestion} & \textbf{Validation focus} & \textbf{Evidence sought} \\
\midrule
\textbf{SQ1} & Mechanism execution & Correct triggering and execution of the registered Attention and WM rules \\
\textbf{SQ2} & Stage-specific information-state transitions & Traceable changes at the stage where each mechanism is intended to operate \\
\textbf{SQ3} & Cumulative and downstream propagation & Evidence that upstream information changes propagate to the resulting Explicit LTM and downstream question performance \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{SQ1---Mechanism Execution.}

\begin{quote}
\textbf{Do the Attention and Working-Memory mechanisms execute their registered rules correctly?}
\end{quote}

SQ1 examines whether the two manipulated ADHD-related mechanisms operate according to their frozen rules. For the Attention Filter, this requires confirmation that an eligible distractor triggers according to the condition-specific setting, that the predefined target information becomes unavailable when triggered, and that non-target instructional information remains unaffected. For Working-Memory Processing, it requires confirmation that frozen round-level processing demand triggers WM overflow according to the registered capacity threshold and that, once triggered, the predefined within-round FIFO-style restriction is applied to information that remains available after Attention. This layer evaluates \textit{mechanism implementation fidelity}: before any learning outcome is interpreted, the observed information changes must be shown to arise from the intended mechanisms rather than from an implementation error or unintended information selection.

\paragraph{SQ2---Stage-Specific Information-State Transitions.}

\begin{quote}
\textbf{Do the individual processing stages produce the intended and attributable changes in information availability and encoding?}
\end{quote}

After mechanism execution has been established, SQ2 examines whether the mechanisms produce observable information-state changes at their intended positions in the CPB pipeline. The Teaching Phase is represented as:

\[
S_r
\xrightarrow{\text{Attention Filter}}
P_{r,c}
\xrightarrow{\text{Working-Memory Processing}}
A_{r,c}
\xrightarrow{\text{Shared Knowledge Encoder}}
L_{r,c}.
\]

Attention operates on the transition from $S_r$ to $P_{r,c}$ and determines which instructional information remains available after controlled distraction. WM subsequently operates on the transition from $P_{r,c}$ to $A_{r,c}$ and further restricts the information that can reach the Encoder under the registered processing-capacity constraint. The shared Knowledge Encoder then transforms $A_{r,c}$ into the round-level Explicit LTM entry $L_{r,c}$. SQ2 therefore asks whether each transition can be observed separately and attributed to its corresponding mechanism, rather than inferred retrospectively from the final LTM or assessment score.

\paragraph{SQ3---Cumulative and Downstream Propagation.}

\begin{quote}
\textbf{Do stage-specific information changes propagate through the full CPB pipeline to the final Explicit LTM and assessment performance?}
\end{quote}

SQ3 examines whether local information-state changes propagate through the complete processing chain. At the aggregate level, enabling either Attention or WM should reduce the amount of Source information reaching the Encoder, while A1W1 should exhibit the cumulative result of both local restrictions. Relative to the encoding-only A0W0 reference, conditions with stronger upstream restriction are expected to show lower Source-Word Availability Ratio (SWAR) and lower Source-to-LTM semantic correspondence, measured by End-to-End Semantic Recall (E2ESR). If these changes continue into the Assessment Phase, aggregate Checklist Scores should also decrease. Strict monotonicity is not required for every question because questions depend on different source rounds and answers may receive partial credit from information that remains in the Explicit LTM.

Study 1 consequently implements the following progressive validation structure:

\[
\begin{aligned}
\text{Mechanism Execution}
&\rightarrow
\text{Stage-Specific Information-State Transitions} \\
&\rightarrow
\text{Cumulative and Downstream Propagation}.
\end{aligned}
\]

Figure~\ref{fig:study1-validation-logic} locates the three subquestions within the CPB pipeline. SQ1 concerns execution of the Attention Filter and Working-Memory Processing; SQ2 concerns the stage-specific transitions produced by Attention, WM, and Encoding; and SQ3 concerns cumulative propagation to the Explicit LTM and final answer. The figure identifies the scope of each subquestion without introducing the specific evaluation measures, which are defined in Section~3.5.2.3.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,keepaspectratio]{Figures/Chapter3/export/ch03_study1_validation_logic.pdf}
    \caption{Study 1 validation structure across mechanism execution, stage-specific information-state transitions, and cumulative downstream propagation.}
    \label{fig:study1-validation-logic}
\end{figure}

This structure prevents RQ1 from depending on a single final performance measure. It separately examines whether the mechanisms execute correctly, whether information changes occur at the intended stages, and whether those local changes propagate to the Explicit LTM and observable answer performance.

\subsubsection{Experimental Design and Conditions}
\label{subsubsec:study1-conditions}

Study 1 uses a $2 \times 2$ factorial ablation design that independently manipulates the OFF/ON states of the Attention Filter and Working-Memory Processing Capacity:

\[
\text{Attention OFF/ON}
\times
\text{WM OFF/ON}.
\]

The four conditions are shown in Table~\ref{tab:study1-conditions}. The design is not intended to construct levels of ADHD severity. Instead, deterministic boundary conditions maximise \textit{mechanism identifiability}: A1W0 and A0W1 isolate the Attention and WM constraints, respectively; A1W1 examines their sequential combination in the registered CPB processing order; and A0W0 provides a reference condition without Attention- or WM-attributed information restriction.

\begin{table}[H]
\centering
\caption{Study 1 experimental conditions.}
\label{tab:study1-conditions}
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{p{0.14\textwidth} p{0.18\textwidth} p{0.22\textwidth} p{0.37\textwidth}}
\toprule
\textbf{Condition} & \textbf{Attention} & \textbf{Working Memory} & \textbf{Functional role} \\
\midrule
\textbf{A0W0} & OFF & OFF & Encoding-only reference \\
\textbf{A1W0} & ON & OFF & Isolates the Attention constraint \\
\textbf{A0W1} & OFF & ON & Isolates the WM constraint \\
\textbf{A1W1} & ON & ON & Tests the sequential combination of Attention and WM constraints \\
\bottomrule
\end{tabular}
\end{table}

Deterministic boundary parameters implement the OFF/ON manipulation. Attention OFF corresponds to $p_A=0$, so distractor events do not trigger Attention-attributed target removal. Attention ON corresponds to $p_A=1$, so every Teaching Round with an eligible distractor assignment triggers the Attention Filter according to the frozen distractor-to-target mapping. The Attention manipulation changes only which instructional information forms the Post-Attention Input $P_{r,c}$; it does not alter the execution rules of downstream mechanisms.

The WM OFF/ON states are implemented through condition-specific capacity thresholds. WM OFF uses a non-binding capacity above the complete frozen PDB range:

\[
C^{WM}_{\mathrm{OFF}}=10^{10}\ \text{bits},
\]

so no Teaching Round triggers WM overflow. WM ON uses the frozen threshold:

\[
C^{WM}_{\mathrm{ON}}=321.9937895\ \text{bits}.
\]

This value is the median boundary of the source-round PDB distribution associated with the 42 independent assessment questions: 21 of the 42 source rounds lie above the capacity and 21 lie at or below it. WM ON therefore does not imply removal in every Teaching Round. Instead, it enables the deterministic capacity rule:

\[
D(R_r)>C^{WM}_{\mathrm{ON}}
\quad\Rightarrow\quad
\text{WM constraint triggered}.
\]

When the rule is satisfied, WM further restricts the instructional information that remains available after Attention according to the procedure defined in Section~\ref{sec:wm-mechanism-implementation}. Rounds at or below the threshold produce no WM-attributed information loss. Attention ON therefore means that every eligible distractor triggers the Attention mechanism, whereas WM ON means that the processing-capacity constraint is enabled but triggers only for rounds whose demand exceeds the threshold.

Under A1W1, the two mechanisms execute sequentially in the registered CPB order:

\[
S_r
\xrightarrow{\text{Attention Filter}}
P_{r,c}
\xrightarrow{\text{Working-Memory Processing}}
A_{r,c}.
\]

Attention first determines which instructional sentences remain available after distraction. WM can then restrict only this remaining information. Its trigger decision still uses the Teaching Round's frozen $D(R_r)$; PDB is not recalculated after Attention removal. A1W1 therefore represents the cumulative operation of two registered mechanisms in a fixed order rather than a separate joint mechanism.

All other experimental components are held constant across the four conditions. They use the same frozen Teaching Rounds, distractor assignments, assessment questions, scoring rubrics, student-model configuration, and Judge procedure, and they share the same Knowledge Encoder. Although A0W0 disables the Attention and WM constraints, it still undergoes normal Knowledge Encoding and is therefore an \textit{encoding-only reference}, not a perfect-memory condition.

Study 1 uses seven frozen instructional materials containing 83 Teaching Rounds and 201 instructional sentences. Lessons L01--L07 contain 14, 12, 11, 11, 12, 10, and 13 Teaching Rounds, respectively. The assessment set is identical in every condition and contains 42 independent questions (six per lesson) and seven cross-round integrative questions (one per lesson), giving 49 questions in total.

Each of the four conditions is executed once as the canonical simulated-learner run. Every condition processes all 83 Teaching Rounds and stores one memory-state record per round, producing 332 condition-round records. Of these, 323 contain encoded Explicit LTM content; the remaining nine are marked \texttt{no\_visible\_information} because no instructional information remained available for encoding. Each condition then answers all 49 questions independently, producing 196 student answers, 196 question-level Judge records, and 548 criterion-level judgements.

These quantities also define the units of analysis. ATRF and NTPR are based on sentence-level Attention decisions, WMTA on all Teaching Rounds, and FRF on eligible WM-overflow rounds. AAR, WMAR, SWAR, ESR, and E2ESR are calculated from round-level processing records and then aggregated into seven lesson-level observations. Checklist Score is first calculated at question level and then aggregated by lesson and condition. Reported 95\% confidence intervals use lesson as the bootstrap unit and therefore reflect variation across the seven frozen lessons rather than stochastic variation across repeated student runs.

\subsubsection{Evaluation Measures}

Study 1 uses three linked layers of evaluation. The SQ1 measures test whether Attention and WM trigger and execute according to the frozen rules. The SQ2 measures quantify the local information-state transitions in $S_r \rightarrow P_{r,c} \rightarrow A_{r,c} \rightarrow L_{r,c}$. The SQ3 measures assess whether those local changes accumulate and propagate to the final Explicit LTM and assessment performance. Unless otherwise stated, availability measures use deterministic $\operatorname{WordCount}(X)$ values that include instructional content only and exclude distractor labels, control markers, and experimental prompt text. These measures describe the amount of instructional information available at each processing stage; they are not interpreted as percentages of human attention, working-memory capacity, or knowledge retention.

\paragraph{SQ1---Mechanism Execution.}

SQ1 first checks whether the manipulated mechanisms implement the rules defined in Section~3.4. In Study 1, Attention ON uses $p_A=1$, so every Teaching Round with an eligible distractor assignment should trigger the Attention Filter and make its mapped target sentence unavailable. Attention OFF uses $p_A=0$ and should produce no Attention-attributed removal. WM follows the deterministic capacity rule $D(R_r)>C^{WM}_c$ and, when triggered, removes the earliest instructional sentence that remains available after Attention. The SQ1 measures therefore test both whether each mechanism triggers when it should and whether it applies the correct information-selection rule after triggering.

Attention execution is assessed using two complementary measures. \textbf{Attention Target-Removal Fidelity (ATRF)} quantifies the proportion of expected Attention targets that are actually removed:

\[
ATRF_c=
\frac{
\sum_r
\left|
T^{\mathrm{expected}}_{r,c}
\cap
T^{\mathrm{removed}}_{r,c}
\right|
}{
\sum_r
\left|
T^{\mathrm{expected}}_{r,c}
\right|
}.
\]

Here, $T^{\mathrm{expected}}_{r,c}$ denotes the sentence identifiers that should be removed under the frozen distractor mapping and trigger setting for condition $c$, and $T^{\mathrm{removed}}_{r,c}$ denotes the identifiers actually removed in the execution log. Values closer to 1 indicate more complete execution of the intended target removal; $ATRF_c=1$ means that all expected targets were correctly removed. Because Attention OFF has no expected target removals, ATRF applies to rounds and conditions in which removal is expected.

The complementary \textbf{Non-Target Preservation Rate (NTPR)} checks whether information that should remain available after Attention is preserved. Let $N^{\mathrm{expected}}_{r,c}$ denote instructional sentences expected to remain available and $N^{\mathrm{preserved}}_{r,c}$ the sentences actually preserved. Then:

\[
NTPR_c=
\frac{
\sum_r
\left|
N^{\mathrm{expected}}_{r,c}
\cap
N^{\mathrm{preserved}}_{r,c}
\right|
}{
\sum_r
\left|
N^{\mathrm{expected}}_{r,c}
\right|
}.
\]

Values closer to 1 indicate fewer unintended Attention removals; $NTPR_c=1$ means that all non-target information was correctly preserved. ATRF and NTPR therefore assess correct removal and correct preservation separately.

WM execution is also assessed using two complementary measures. \textbf{Working-Memory Trigger Agreement (WMTA)} quantifies whether the trigger status in the execution log agrees with the registered capacity rule:

\[
WMTA_c
=
\frac{1}{R}
\sum_r
\mathbf{1}
\left[
z^{WM}_{r,c}
=
\mathbf{1}
\left[
D(R_r)>C^{WM}_c
\right]
\right],
\]

where $z^{WM}_{r,c}$ is the recorded WM trigger status. Values closer to 1 indicate stronger agreement between observed triggering and the threshold rule; $WMTA_c=1$ means that every Teaching Round triggered exactly when required.

For rounds in which WM triggers and the Post-Attention Input still contains removable instructional information, \textbf{FIFO Rule Fidelity (FRF)} is calculated. Let $\mathcal E_c$ denote this set of eligible rounds:

\[
FRF_c=
\frac{1}{|\mathcal E_c|}
\sum_{r\in\mathcal E_c}
\mathbf{1}
\left[
R^{WM}_{r,c}
=
\{\operatorname{First}(P_{r,c})\}
\right].
\]

Values closer to 1 indicate more consistent execution of the predefined within-round FIFO-style information-restriction rule; $FRF_c=1$ means that the earliest available instructional sentence was correctly removed in every eligible overflow round. ATRF, NTPR, WMTA, and FRF are implementation-fidelity measures defined from the frozen mechanism rules and execution logs. They verify computational execution rather than human ADHD cognitive fidelity.
When $|\mathcal E_c|=0$, FRF is undefined and is reported as N/A rather than assigned a numerical value.

\paragraph{SQ2---Stage-Specific Information-State Transitions.}

After mechanism execution has been established, SQ2 evaluates information-state changes at the transitions:

\[
S_r
\xrightarrow{\text{Attention}}
P_{r,c}
\xrightarrow{\text{WM}}
A_{r,c}
\xrightarrow{\text{Encoding}}
L_{r,c}.
\]

The Attention stage uses the \textbf{Attention-stage Availability Ratio (AAR)} to quantify the proportion of Source information that remains available after the Attention Filter. For lesson $l$ and condition $c$:

\[
AAR_{l,c}
=
\frac{
\sum_{r\in l}\operatorname{WordCount}(P_{r,c})
}{
\sum_{r\in l}\operatorname{WordCount}(S_r)
}.
\]

$AAR_{l,c}=1$ means that no instructional information in the lesson became unavailable at the Attention stage; lower values indicate greater Attention-attributed loss. AAR is an information-availability ratio computed from the explicit processing trace, not an ``attention percentage''.

The WM stage uses the \textbf{Working-Memory-stage Availability Ratio (WMAR)} to quantify the proportion of Post-Attention Input that passes the WM constraint and reaches the Knowledge Encoder:

\[
WMAR_{l,c}
=
\frac{
\sum_{r\in l}\operatorname{WordCount}(A_{r,c})
}{
\sum_{r\in l}\operatorname{WordCount}(P_{r,c})
}.
\]

Using $P_{r,c}$ rather than the complete Source as the denominator separates WM-attributed restriction from preceding Attention loss. $WMAR_{l,c}=1$ indicates that WM caused no additional reduction in the Post-Attention Input; lower values indicate greater restriction among the information that passed Attention. AAR and WMAR therefore localise the information-availability effects of Attention and WM, respectively.

Knowledge Encoding is shared across conditions and is not an additional ADHD-specific manipulation. This stage is evaluated through \textbf{Encoding Semantic Recall (ESR)}, a diagnostic of semantic correspondence between the Available Input and the generated Explicit LTM:

\[
ESR_{l,c}
=
\frac{1}{|\mathcal R^+_{l,c}|}
\sum_{r\in\mathcal R^+_{l,c}}
B_R(A_{r,c},L_{r,c}),
\]

where:

\[
\mathcal R^+_{l,c}
=
\left\{
r\in l:
\operatorname{WordCount}(A_{r,c})>0
\right\}.
\]

$B_R(A_{r,c},L_{r,c})$ is BERTScore Recall calculated with Available Input as the reference and Explicit LTM as the candidate. BERTScore uses contextual embeddings to match candidate and reference tokens and can therefore identify paraphrases and semantically similar expressions that exact lexical-overlap measures may miss \cite{zhang2019bertscore}. The frozen configuration uses the \texttt{microsoft/deberta-xlarge-mnli} model, with Recall as the reported component, inverse-document-frequency weighting disabled, and baseline rescaling enabled.

Recall is used because the CPB Encoder is instructed to compress and paraphrase the Available Input into a short LTM entry. The relevant question is how much of the semantic content reaching the Encoder is represented in the generated LTM, not whether input and output are lexically identical or equally long. Higher ESR indicates stronger semantic coverage of the received Available Input; lower ESR indicates that more input semantics lack a corresponding representation in the LTM. Because baseline rescaling is applied, ESR is not interpreted as a strict probability on a 0--1 retention scale. Rounds with an empty $A_{r,c}$ are excluded because they contain no instructional content for the Encoder to transform.
If $\mathcal R^+_{l,c}$ is empty, ESR is undefined for that lesson--condition combination and is reported as N/A.

\paragraph{SQ3---Cumulative and Downstream Propagation.}

SQ3 moves from local transitions to the complete processing chain and tests whether Attention- and WM-attributed changes accumulate in the information reaching the Encoder, the resulting Explicit LTM, and Assessment Phase performance.

The \textbf{Source-Word Availability Ratio (SWAR)} quantifies the proportion of the original Source that remains available to the Encoder after both Attention and WM:

\[
SWAR_{l,c}
=
\frac{
\sum_{r\in l}\operatorname{WordCount}(A_{r,c})
}{
\sum_{r\in l}\operatorname{WordCount}(S_r)
}.
\]

Values closer to 1 indicate that a larger proportion of the Source reaches the shared Knowledge Encoder; lower values indicate stronger cumulative upstream restriction. Under the same lesson-level aggregation:

\[
SWAR_{l,c}
=
AAR_{l,c}
\times
WMAR_{l,c}.
\]

SWAR is therefore not a separate cognitive mechanism measure, but the cumulative information-availability outcome of the Attention and WM stages.

As a supplementary decomposition, the frozen Source-word total in each condition is partitioned into Available Input, Attention-attributed loss, and WM-attributed loss:

\[
W_{\text{source},c}
=
W_{\text{available},c}
+
W_{\text{attention-loss},c}
+
W_{\text{WM-loss},c}.
\]

$W_{\text{available},c}$ is the total word count of $A_{r,c}$ across Teaching Rounds, and $W_{\text{attention-loss},c}$ and $W_{\text{WM-loss},c}$ are the cumulative word losses attributed to the two mechanisms. This decomposition supplements SWAR by showing the absolute composition and attribution of upstream information restriction.

\textbf{End-to-End Semantic Recall (E2ESR)} quantifies semantic correspondence between the complete instructional Source and the final Explicit LTM:

\[
E2ESR_{l,c}
=
\frac{1}{R_l}
\sum_{r\in l}
B_R(S_r,L_{r,c}),
\]

where $R_l$ is the number of Teaching Rounds in lesson $l$. The same frozen BERTScore Recall configuration is used \cite{zhang2019bertscore}. ESR evaluates $A_{r,c}\rightarrow L_{r,c}$ and therefore diagnoses how the Encoder represents the information it actually receives. E2ESR evaluates $S_r\rightarrow L_{r,c}$ and describes how much Source semantics remains represented after Attention, WM, and Encoding. If a round produces no LTM entry, its E2ESR contribution is set to zero so that complete upstream information loss is retained in the cumulative measure. Higher E2ESR indicates stronger semantic correspondence between final Explicit LTM and the original instructional content; E2ESR is not interpreted as a percentage of human knowledge retention.

Finally, \textbf{Checklist Score} is used as a downstream supporting outcome from the complete Source to the final answer. The questions, frozen atomic criteria, source evidence, and criterion-level Judge procedure are defined in Sections~\ref{subsec:rubric-construction} and~\ref{subsec:judge-validation}. For question $q$ with $K_q$ criteria:

\[
\operatorname{ChecklistScore}_{q,c}
=
10
\times
\frac{
\sum_{k=1}^{K_q}
\mathbf{1}
\left[
\ell_{q,k,c}=\mathrm{Correct}
\right]
}{
K_q
}.
\]

$\ell_{q,k,c}$ is the Judge label for criterion $k$ under condition $c$. \texttt{Absent} and \texttt{Contradicted} both receive zero credit but remain separate diagnostic labels. Checklist Score does not directly validate whether Attention or WM executed correctly. It tests whether the preceding mechanism execution and information-state changes propagate to final assessment performance and therefore serves as downstream supporting evidence for SQ3 rather than independent mechanistic evidence for RQ1.

Table~\ref{tab:study1-measures} summarises the relationship between the three subquestions and their measures.

\begin{table}[H]
\centering
\caption{Evaluation measures for Study 1.}
\label{tab:study1-measures}
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{p{0.12\textwidth} p{0.21\textwidth} p{0.15\textwidth} p{0.44\textwidth}}
\toprule
\textbf{Subquestion} & \textbf{Validation target} & \textbf{Measure} & \textbf{Interpretation} \\
\midrule
\textbf{SQ1} & Attention execution & ATRF / NTPR & Whether intended Attention targets are removed while non-target information is preserved \\
\textbf{SQ1} & WM execution & WMTA / FRF & Whether WM triggers at the registered threshold and applies the correct within-round removal rule \\
\textbf{SQ2} & $S_r\rightarrow P_{r,c}$ & AAR & Proportion of Source information remaining available after Attention \\
\textbf{SQ2} & $P_{r,c}\rightarrow A_{r,c}$ & WMAR & Proportion of Post-Attention information remaining available after WM \\
\textbf{SQ2} & $A_{r,c}\rightarrow L_{r,c}$ & ESR & Semantic correspondence between actual Encoder input and generated Explicit LTM \\
\textbf{SQ3} & $S_r\rightarrow A_{r,c}$ & SWAR & Cumulative proportion of Source information reaching the Encoder \\
\textbf{SQ3} & $S_r\rightarrow L_{r,c}$ & E2ESR & End-to-end semantic correspondence between Source and Explicit LTM \\
\textbf{SQ3} & Full pipeline to answer & Checklist Score & Proportion of frozen assessment criteria correctly expressed in the final answer \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Expected Patterns and Interpretation}

Study 1 interprets its results through the progressive SQ1--SQ3 structure: mechanism execution, stage-specific transitions, and cumulative propagation. All expectations concern aggregate-level patterns. Apart from deterministic implementation-fidelity checks, the study does not require every lesson or question to exhibit the same ordering.

\begin{table}[H]
\centering
\caption{Prespecified aggregate-level expectations for Study 1.}
\label{tab:study1-expectations}
\scriptsize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{p{0.25\textwidth} p{0.67\textwidth}}
\toprule
\textbf{Subquestion and target} & \textbf{Prespecified aggregate-level expectation} \\
\midrule
\textbf{SQ1---Mechanism execution} & All applicable fidelity measures should equal 1.000, with no Attention or WM implementation violations. \\
\textbf{SQ2---Attention stage} & Attention ON should reduce AAR. Holding Attention constant, changing the downstream WM setting should not alter AAR. Thus, $AAR_{A0W0}=AAR_{A0W1}$ and $AAR_{A1W0}=AAR_{A1W1}$ are expected, with Attention ON lower than the corresponding Attention OFF condition. \\
\textbf{SQ2---WM stage} & WM OFF should produce $WMAR=1$. WM ON should introduce additional restriction only in rounds whose processing demand exceeds the frozen threshold, producing lower aggregate WMAR than the corresponding WM OFF condition. \\
\textbf{SQ2---Shared Encoding} & Because all conditions use the same Encoder, no strict ESR ordering is prespecified; systematic divergence caused by a condition-specific Encoding procedure is not expected. \\
\textbf{SQ3---Cumulative propagation} & A1W1 should contain both local changes and exhibit the lowest or near-lowest aggregate Source availability. Relative to A0W0, stronger upstream restriction should correspond to lower SWAR and E2ESR. \\
\textbf{SQ3---Downstream outcome} & Checklist Score should indicate whether upstream changes propagate to assessment performance. Aggregate scores are expected to decrease as cumulative restriction increases, without requiring strict monotonicity for every question or lesson. \\
\bottomrule
\end{tabular}
\end{table}

A0W0 still undergoes shared Knowledge Encoding and is therefore an encoding-only reference rather than a perfect-memory baseline. Checklist Score remains supporting evidence for cumulative propagation. Mechanistic validity for Attention and WM is established primarily through SQ1 execution fidelity and SQ2 stage localisation.

\subsection{Study 2---Comparative Representational Validity}
\label{subsec:study2-design}

\subsubsection{Research Question and Subquestions}
\label{subsubsec:study2-rq}

Study 2 addressed RQ2 by comparing the behavioural sensitivity of persona prompting and CPB under controlled distraction and instructional processing demand. RQ2 was:

\begin{quote}
\small
\textbf{RQ2---Representation:} Compared with persona prompting, does CPB produce learning behaviour that shows more theoretically consistent sensitivity to controlled distraction and processing demand?
\end{quote}

RQ2 concerns both differentiation within each representation and systematic responses to theoretically relevant external manipulations. A comparison of overall mean scores alone would therefore be insufficient. Study 2 decomposed RQ2 into four linked subquestions. SQ1 and SQ2 tested whether persona prompting and CPB, respectively, produced interpretable differentiation within their own representation. SQ3 and SQ4 then compared their sensitivity to controlled distraction and instructional processing demand. Table~\ref{tab:study2-subquestions} summarises this structure.

\begin{table}[H]
\centering
\caption{Subquestions and validation focus for Study 2.}
\label{tab:study2-subquestions}
\scriptsize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{p{0.12\textwidth} p{0.23\textwidth} p{0.52\textwidth}}
\toprule
\textbf{Subquestion} & \textbf{Validation focus} & \textbf{Evidence sought} \\
\midrule
\textbf{SQ1} & Persona differentiation & Whether Prompt-NT, Moderate-Intensity Prompt-ADHD, and High-Intensity Prompt-ADHD produce distinguishable learning behaviour \\
\textbf{SQ2} & Graded CPB response & Whether increasing CPB constraint severity produces a systematic ordinal change in learning performance \\
\textbf{SQ3} & Distraction sensitivity & Whether the two representations differ systematically in their behavioural response to controlled distraction \\
\textbf{SQ4} & Processing-demand sensitivity & Whether the two representations differ systematically in their sensitivity to instructional processing demand \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{SQ1---Persona Differentiation.}

\begin{quote}
\small
\raggedright
\textbf{Does persona prompting produce distinguishable learning behaviour across Prompt-NT and the two Prompt-ADHD intensity conditions?}
\end{quote}

SQ1 tested whether persona-level learner descriptions alone produced stable differentiation in final learning behaviour. The persona branch comprised one Prompt-NT learner and two learners with different ADHD-description intensities: Moderate-Intensity Prompt-ADHD and High-Intensity Prompt-ADHD. Under matched teaching materials, questions, and scoring, the analysis examined both NT--ADHD differentiation and whether the two ADHD intensities produced distinguishable behavioural patterns. If the three Prompt learners differed only in language style or phrasing without stable performance differentiation, this would indicate weak behavioural representation of ADHD-related learning characteristics and their intensity. SQ1 therefore established an internal persona-representation baseline without requiring the observed differences to be localisable to an explicit cognitive-processing stage.

\paragraph{SQ2---Graded CPB Response.}

\begin{quote}
\small
\textbf{Does increasing CPB constraint severity produce a systematic graded change in learning performance?}
\end{quote}

SQ2 tested whether the factorised CPB representation produced an ordered behavioural response as registered constraint severity increased. Study 2 used CPB Zero, Low, Medium, and High conditions, with Attention susceptibility and WM capacity jointly parameterised to impose progressively stronger constraints. The relevant evidence was not whether any single condition produced a low score, but whether Zero $\rightarrow$ Low $\rightarrow$ Medium $\rightarrow$ High formed an interpretable ordinal pattern in aggregate learning performance. Such a pattern would establish a systematic correspondence between CPB behavioural output and its internal constraint severity.

\paragraph{SQ3---Distraction Sensitivity.}

\begin{quote}
\small
\textbf{How do persona prompting and CPB differ in their sensitivity to controlled distraction?}
\end{quote}

SQ3 compared behavioural change between clean and distracted materials. Within persona prompting, it examined whether Prompt-NT and the two Prompt-ADHD intensities showed distinguishable changes in performance under distraction. Within CPB, it examined whether the response of Zero, Low, Medium, and High varied systematically with constraint severity. The focus was not which representation produced the lower absolute score, but whether its output showed stable sensitivity to controlled distraction that corresponded to the representation design.

\paragraph{SQ4---Processing-Demand Sensitivity.}

\begin{quote}
\small
\textbf{How do persona prompting and CPB differ in their sensitivity to instructional processing demand?}
\end{quote}

SQ4 compared the relationship between instructional processing demand and performance across the two representations. For persona prompting, it tested whether Prompt-NT and the two Prompt-ADHD intensities showed different relationships between source-round demand and question performance. For CPB, it tested whether higher-demand instructional content produced a larger performance cost as registered constraint severity increased. Because processing demand principally enters the implemented framework through the WM-related capacity rule, SQ4 assessed whether either representation produced a systematic response to this controlled theoretical variable.

Study 2 therefore moved from differentiation within each representation to comparison of their behavioural sensitivities:

\[
\begin{aligned}
\text{Persona Differentiation}
&\rightarrow \text{Graded CPB Response} \\
&\rightarrow \text{Distraction Sensitivity} \\
&\rightarrow \text{Processing-Demand Sensitivity}.
\end{aligned}
\]

RQ2 did not depend on one representation producing lower absolute performance than the other. It tested whether differentiation was established within each representation and whether the resulting behavioural outputs responded systematically and theory-consistently to controlled distraction and instructional processing demand.

\subsubsection{Experimental Design and Conditions}
\label{subsubsec:study2-conditions}

Study 2 used a fully crossed learner-condition $\times$ material-condition design. The learner factor comprised three persona-prompted and four CPB learners. Each learner condition received both clean and distracted materials, with five independent runs in every learner--material cell.

The persona-prompted branch comprised Prompt-NT and two Prompt-ADHD intensity conditions. The Prompt-ADHD descriptions used the text-based educational prompts provided by Gonnermann-Müller et al. \cite{gonnermann-muller-etal-2026-adhd-persona}. They retained the same ADHD-related behavioural content while varying intensity through frequency terms. The Moderate-Intensity condition primarily used terms such as \textit{sometimes}, \textit{occasionally}, and \textit{somewhat}; the High-Intensity condition primarily used terms such as \textit{often}, \textit{frequently}, and \textit{regularly}. Prompt-NT contained no ADHD-specific cognitive description and served as the persona-prompted reference condition.

The CPB branch comprised Zero, Low, Medium, and High. CPB Zero retained Teaching-Round processing, shared Knowledge Encoding, Explicit LTM accumulation, and memory-constrained answering while disabling both information-loss mechanisms. It therefore served as the zero-constraint CPB reference. Low, Medium, and High jointly varied Attention-trigger probability and WM-capacity threshold to impose progressively stronger constraints. These configurations are experimental parameters and do not represent clinical ADHD severity. Table~\ref{tab:study2-conditions} lists all learner conditions.

\begin{table}[H]
\centering
\caption{Learner conditions used in Study 2.}
\label{tab:study2-conditions}
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{p{0.18\textwidth} p{0.14\textwidth} p{0.25\textwidth} p{0.14\textwidth} p{0.14\textwidth}}
\toprule
\textbf{\makecell{Learner\\condition}} & \textbf{Representation} & \textbf{Manipulation} & \textbf{\makecell{Attention\\probability}} & \textbf{\makecell{WM\\capacity}} \\
\midrule
\textbf{Prompt-NT} & Persona prompting & No ADHD-specific description & N/A & N/A \\
\textbf{Prompt-ADHD---Moderate} & Persona prompting & Moderate-intensity ADHD description & N/A & N/A \\
\textbf{Prompt-ADHD---High} & Persona prompting & High-intensity ADHD description & N/A & N/A \\
\textbf{CPB Zero} & CPB & Attention and WM restrictions disabled & 0.00 & $10^{10}$ bits \\
\textbf{CPB Low} & CPB & Low processing constraint & 0.20 & 362.099 bits ($Q_{2/3}$) \\
\textbf{CPB Medium} & CPB & Medium processing constraint & 0.50 & 321.994 bits ($Q_{1/2}$) \\
\textbf{CPB High} & CPB & High processing constraint & 0.80 & 246.930 bits ($Q_{1/3}$) \\
\bottomrule
\end{tabular}
\end{table}

Both material conditions were derived from the same seven frozen teaching materials. The clean condition used instructional text without distractor events. The distracted condition inserted the frozen events at the predetermined locations. Apart from these events, the two versions retained the same instructional content, Teaching-Round boundaries, sentence identifiers, and ordering. The material manipulation therefore did not alter the curriculum topic, assessment scope, or instructional structure.

Persona-prompted and CPB learners received the same Teaching-Round sequence but followed different representation pipelines. For persona prompting, each round was appended to the complete conversation history in source order and followed by a fixed \texttt{OK} acknowledgement. After teaching, each assessment question branched independently from the same frozen post-exposure history so that earlier questions and answers could not supply information to later questions. This branch did not generate a separate Knowledge-Encoding entry or Explicit LTM.

For CPB learners, each Teaching Round passed through Attention, WM processing, and shared Knowledge Encoding. The Attention trigger was sampled using the condition-specific probability and stored random seed. When triggered, the frozen target information became unavailable through hard deletion. The WM mechanism then processed the Post-Attention Input using the condition-specific capacity threshold, after which the shared Encoder converted the resulting Available Input into a round-level Explicit LTM entry. During assessment, the learner received only the Explicit LTM generated for that lesson; the complete instructional material was not reintroduced.

Both branches used DeepSeek V4-Flash as the student model and shared the frozen materials, assessment questions, and scoring framework. Their assessment inputs followed their respective representation architectures: Prompt learners answered from the frozen post-exposure conversation context, whereas CPB learners answered only from the frozen Explicit LTM. Each lesson contained six independent questions and one cross-round integrative question, giving 42 independent and seven integrative questions across the seven lessons.

The seven learner conditions were crossed with the two material conditions and run five times per cell:

\[
7\ \text{learner conditions}
\times 2\ \text{material conditions}
\times 5\ \text{runs}
=70\ \text{simulated-learner runs}.
\]

Because every run covered seven lessons and all 49 questions, Study 2 produced

\[
70\times7=490
\]

lesson-level learning sessions and

\[
70\times49=3{,}430
\]

assessment answers. Each run used a unique identifier. CPB records additionally preserved the Attention-sampling seed, realised trigger decisions, WM-processing records, Explicit LTM, and answer files, making each stochastic Attention trajectory reproducible. Prompt records preserved the complete round-wise classroom history and independent answer record for each question.

All 3,430 answers were scored by the same frozen \texttt{qwen3.7-max} criterion-level LLM Judge. The Judge received only the frozen question, reference answer, atomic checklist criteria, source evidence, and student answer. Learner identity, representation condition, material condition, and run label were withheld. Criterion-level results were rejoined with local experimental metadata only after scoring. Section~\ref{subsubsec:study2-measures} defines the measures used to address the four subquestions.

\subsubsection{Evaluation Measures}
\label{subsubsec:study2-measures}

The four Study 2 subquestions shared a common set of outcome measures. Rather than defining a separate learning-performance metric for every subquestion, the analysis used the same question-level scores to examine persona differentiation, graded CPB response, distraction sensitivity, and processing-demand sensitivity from different comparison perspectives. The five independent runs of each question within a learner and material condition were first reduced to a question-level mean Checklist Score and a run-to-run standard deviation (SD). Response word count was retained as a descriptive answer characteristic; it was not interpreted as learning performance.

For question $q$, learner condition $c$, material condition $m$, and run $j$, let the Checklist Score be $S_{q,c,m,j}$. The question-level mean was

\[
\bar S_{q,c,m}
=
\frac{1}{5}
\sum_{j=1}^{5}
S_{q,c,m,j}.
\]

$\bar S_{q,c,m}$ represents mean assessment performance on question $q$ under the specified learner and material condition. All higher-level summaries were calculated from these question-level means, including the overall mean across 49 questions, lesson-level means, the mean across 42 independent questions, and the mean across seven cross-round integrative questions. The aggregation order was therefore

\[
\begin{aligned}
\text{Run-level Score}
&\rightarrow \text{Question-level Mean} \\
&\rightarrow \text{Lesson / Question-Type / Overall Summary}.
\end{aligned}
\]

Run-to-run stability was quantified using the question-level sample SD:

\[
SD_{q,c,m}
=
\sqrt{
\frac{1}{5-1}
\sum_{j=1}^{5}
\left(S_{q,c,m,j}-\bar S_{q,c,m}\right)^2
}.
\]

Because SD and Checklist Score use the same scale, SD directly expresses variation in performance across the five runs of the same question. Lower values indicate greater run-to-run stability. Question-level SDs were subsequently summarised by lesson, question type, or across all questions. Variance was not additionally reported because it would duplicate the same stability information on a squared scale.

Mean answer word count was also calculated across the five runs of each question. This descriptive measure was used to identify systematic differences in response length and to determine whether score differences were accompanied by changes in verbosity. It was not treated as a direct indicator of knowledge acquisition or learning effectiveness.

\paragraph{SQ1---Persona Differentiation.}

SQ1 compared the question-level mean Checklist Scores of Prompt-NT, Moderate-Intensity Prompt-ADHD, and High-Intensity Prompt-ADHD separately under clean and distracted materials. Overall mean performance and mean question-level SD first described aggregate differentiation and run-to-run stability. Scores and SDs were then summarised by lesson and by independent versus integrative question type to assess whether any differentiation was distributed across materials and question structures rather than driven by a single lesson.

The \textbf{Ordering Consistency Rate (OCR)} quantified persona-intensity ordering. For material condition $m$, the expected Prompt ordering was

\[
\bar S_{q,\mathrm{NT},m}
\ge
\bar S_{q,\mathrm{Moderate},m}
\ge
\bar S_{q,\mathrm{High},m}.
\]

Prompt OCR was the proportion of eligible questions satisfying this non-increasing ordering:

\[
OCR_{\mathrm{Prompt},m}
=
\frac{
\#\left\{
q:
\bar S_{q,\mathrm{NT},m}
\ge
\bar S_{q,\mathrm{Moderate},m}
\ge
\bar S_{q,\mathrm{High},m}
\right\}
}{Q},
\qquad Q=49.
\]

Values closer to 1 indicate that the Prompt-NT $\rightarrow$ Moderate $\rightarrow$ High intensity ordering was preserved for more questions. Lower OCR indicates a weaker ordinal relationship between persona intensity and question-level performance. Non-strict inequalities were used because the discrete Checklist Score permits ties across conditions.

\paragraph{SQ2---Graded CPB Response.}

SQ2 applied the same outcome summaries to CPB Zero, Low, Medium, and High under both material conditions. The expected non-increasing ordering was

\[
\bar S_{q,\mathrm{Zero},m}
\ge
\bar S_{q,\mathrm{Low},m}
\ge
\bar S_{q,\mathrm{Medium},m}
\ge
\bar S_{q,\mathrm{High},m},
\]

and CPB OCR was defined as

\[
OCR_{\mathrm{CPB},m}
=
\frac{
\#\left\{
q:
\bar S_{q,\mathrm{Zero},m}
\ge
\bar S_{q,\mathrm{Low},m}
\ge
\bar S_{q,\mathrm{Medium},m}
\ge
\bar S_{q,\mathrm{High},m}
\right\}
}{Q},
\qquad Q=49.
\]

Higher CPB OCR indicates a more stable ordinal correspondence between registered constraint severity and question-level performance. This measure also provides the Study 2 baseline ordering against which Study 3 evaluates whether added learner attributes preserve the CPB gradient.

\paragraph{SQ3---Distraction Sensitivity.}

SQ3 used the paired clean and distracted results for each learner condition. For question $q$ and learner condition $c$, distraction cost was defined as

\[
\Delta D_{q,c}
=
\bar S_{q,c,\mathrm{clean}}
-
\bar S_{q,c,\mathrm{distracted}}.
\]

$\Delta D_{q,c}>0$ indicates lower performance under distracted materials, with larger positive values representing a stronger distraction-associated cost. Values near zero indicate little material-related change, while negative values indicate higher performance under distracted materials. Question-level costs were summarised across all questions, by lesson, and by independent versus integrative question type for each of the seven learner conditions.

The analysis also compared question-level SD between clean and distracted materials to describe any accompanying change in run-to-run stability. Clean--distracted change in response word count was retained only as supplementary description of whether behavioural sensitivity coincided with changes in answer length.

\paragraph{SQ4---Processing-Demand Sensitivity.}

SQ4 examined whether question performance changed systematically with instructional processing demand. The primary analysis was restricted to the 42 independent questions because each mapped to one source Teaching Round and one frozen PDB value. Cross-round integrative questions involved multiple Teaching Rounds and therefore had no unique single-round PDB.

The 42 independent questions were ordered by source-round PDB, and the question-level mean Checklist Score profile was visualised for each learner condition. The primary quantitative measure was the PDB--score slope. Consistent with the implemented analysis and reported results, PDB was scaled in units of 100 bits:

\[
\bar S_q
=
\alpha
+
\beta_{\mathrm{PDB}}
\left(\frac{PDB_q}{100}\right)
+
\varepsilon_q.
\]

$\beta_{\mathrm{PDB}}$ therefore denotes the expected Checklist-Score change per 100-bit increase in instructional processing demand. Values near zero indicate little linear association; negative values indicate lower performance for higher-demand source rounds; and more negative values indicate stronger linear demand sensitivity.

As a supporting analysis that does not assume linearity, Spearman's rank correlation $\rho$ was calculated between PDB and question-level mean Checklist Score. A negative value indicates that higher PDB generally corresponds to lower performance, with values closer to $-1$ indicating a more consistent monotonic negative relationship.

The study also reported the predefined Low--High demand gap:

\[
\Delta_{\mathrm{Demand},c}
=
\bar S_{\mathrm{Low\ demand},c}
-
\bar S_{\mathrm{High\ demand},c}.
\]

A positive gap indicates poorer mean performance on High-demand questions. This quantity provides an interpretable descriptive summary; the continuous PDB--score relationship remains the primary demand-sensitivity evidence.

Independent and integrative performance was not treated as a fifth subquestion. It was reported as a common question-structure dimension across SQ1--SQ3. For SQ4, integrative questions were retained only as supplementary question-structure evidence and excluded from the continuous-demand analysis because they do not map to a unique source-round PDB. Table~\ref{tab:study2-evaluation-framework} summarises the comparison framework.

\begin{table}[H]
\centering
\caption{Evaluation framework for Study 2.}
\label{tab:study2-evaluation-framework}
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{p{0.22\textwidth} p{0.20\textwidth} p{0.22\textwidth} p{0.27\textwidth}}
\toprule
\textbf{Subquestion} & \textbf{Core comparison} & \textbf{Primary outcome} & \textbf{Supporting evidence} \\
\midrule
\textbf{SQ1---Persona differentiation} & Prompt-NT $\rightarrow$ Moderate $\rightarrow$ High & Question-level mean Checklist Score & Run-to-run SD, Prompt OCR, lesson and question-type summaries \\
\textbf{SQ2---Graded CPB response} & CPB Zero $\rightarrow$ Low $\rightarrow$ Medium $\rightarrow$ High & Question-level mean Checklist Score & Run-to-run SD, CPB OCR, lesson and question-type summaries \\
\textbf{SQ3---Distraction sensitivity} & Clean $\rightarrow$ Distracted & Distraction cost $\Delta D$ & SD change, lesson and question-type decomposition, response-length change \\
\textbf{SQ4---Processing-demand sensitivity} & Increasing source-round PDB & PDB--score slope $\beta_{\mathrm{PDB}}$ & Spearman $\rho$, Low--High demand gap \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Expected Patterns and Interpretation}
\label{subsubsec:study2-expectations}

Study 2 used the SQ1--SQ4 sequence to evaluate within-representation differentiation, graded CPB response, and behavioural sensitivity to controlled distraction and processing demand. All expectations concerned aggregate patterns. Apart from the explicit ordering measures, the design did not require every question, lesson, or run to follow the same ordering. Lower performance was not treated as inherently more ADHD-like; the relevant evidence was systematic correspondence between behavioural outcomes and the manipulated learner representation, material condition, and processing demand.

\begin{table}[H]
\centering
\caption{Prespecified aggregate-level expectations for Study 2.}
\label{tab:study2-expectations}
\scriptsize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{p{0.26\textwidth} p{0.66\textwidth}}
\toprule
\textbf{Subquestion} & \textbf{Prespecified aggregate-level expectation} \\
\midrule
\textbf{SQ1---Persona differentiation} & Prompt-NT, Moderate-Intensity Prompt-ADHD, and High-Intensity Prompt-ADHD should show distinguishable behavioural patterns. If persona intensity maps to learning performance, stable differences should appear in overall, lesson-level, or question-type summaries and produce a comparatively high Prompt OCR. \\
\textbf{SQ2---Graded CPB response} & CPB Zero, Low, Medium, and High should form an ordered aggregate performance pattern as registered constraint severity increases. Stronger constraints should correspond to Checklist Scores no higher than weaker constraints and produce a comparatively high CPB OCR. \\
\textbf{SQ3---Distraction sensitivity} & Distracted materials should produce a measurable performance change relative to clean materials within the same learner condition. ADHD-related Prompt conditions and stronger CPB constraints should show larger aggregate distraction costs if their representations are theory-consistently sensitive to the manipulation. \\
\textbf{SQ4---Processing-demand sensitivity} & Performance should show a stronger decline as source-round PDB increases. Stronger CPB constraints should correspond to more negative PDB--score slopes and rank associations if the WM-related constraint representation is theory-consistent; Prompt conditions test whether persona intensity produces an analogous differentiation. \\
\bottomrule
\end{tabular}
\end{table}

Independent and integrative performance is reported as a common question-structure dimension for SQ1 and SQ2 and as a decomposition of the distraction effect for SQ3, rather than as a separate fifth subquestion. Run-to-run SD describes stability within learner--material conditions, while response word count remains a descriptive characteristic rather than a direct measure of learning effectiveness.

For SQ3, larger positive $\Delta D$ values indicate stronger distraction-associated performance costs. The cross-representation comparison concerns whether this sensitivity changes systematically with persona intensity or CPB constraint severity, not which representation produces the lower absolute score. For SQ4, more negative $\beta_{\mathrm{PDB}}$ and Spearman $\rho$ values indicate greater performance sensitivity to processing demand. Integrative questions are excluded from the primary continuous-demand analysis because they do not correspond to a unique source-round PDB.

The theoretical-consistency judgement for Study 2 therefore follows the evidence chain

\[
\begin{aligned}
\text{Representation-internal Differentiation}
&\rightarrow \text{Systematic Response to Distraction} \\
&\rightarrow \text{Systematic Response to Processing Demand}.
\end{aligned}
\]

RQ2 does not require CPB to produce lower scores under every condition. It asks whether CPB behavioural patterns correspond more consistently than persona prompting to the explicitly manipulated Attention- and WM-related demands. These expectations also establish the Study 2 behavioural baseline used in Study 3 to test whether additional learner attributes disrupt learning-related performance, stability, and ordering patterns.

\subsection{Study 3---Multidimensional Representational Robustness (Unfinished)}

\subsubsection{Research Question and Subquestions (Unfinished)}

Study 3 addressed RQ3 by examining the extent to which joint persona prompting and a factorised CPB representation preserved the behavioural baseline established in Study 2 after functionally heterogeneous learner-profile prompts were added. RQ3 was:

\begin{quote}
\textbf{RQ3---Multidimensional Robustness:} When additional functionally heterogeneous learner-profile prompts are introduced, does a factorised CPB representation better preserve its Study 2 behavioural outcomes, learner-condition ordering, and processing-demand sensitivity than joint persona prompting?
\end{quote}

Study 3 did not require final scores to remain completely unchanged after the additional attributes were introduced. Robustness was instead quantified at three connected levels. First, the analysis measured displacement in mean performance, run-to-run variability, and response length relative to the matched Study 2 baseline. Second, it examined whether the question-level ordering of matched learner conditions from Study 2 was preserved under the A1--B2 profiles. Third, it tested whether the relationship between performance and source-round Processing Demand Bits (PDB) changed. High or Low Language Ability was crossed with a learning-congruent or learning-challenging Big-Five composite to form four controlled attribute profiles. A1 and A2 were directionally aligned configurations, whereas B1 and B2 were conflicting configurations. Each profile was analysed as a complete prompt addition; separate Language main effects, Big-Five main effects, and their statistical interaction were not estimated.

\[
\begin{array}{c|cc}
 & BF_{\mathrm{congruent}} & BF_{\mathrm{challenging}} \\
\hline
Language_{\mathrm{High}} & A1 & B1 \\
Language_{\mathrm{Low}} & B2 & A2
\end{array}
\]

RQ3 was divided into three subquestions. All three used the same matched baselines from the distracted arm of Study 2 but examined preservation at the outcome, ordering, and processing-demand-relationship levels, respectively.

\paragraph{SQ1---Baseline-Relative Outcome Displacement (Unfinished)}

\begin{quote}
\textbf{How much do mean performance, run-to-run variability, and response length change after Language Ability and Big-Five profile prompts are added, relative to the matched Study 2 baseline?}
\end{quote}

SQ1 paired every Study 3 learner-type--profile condition with the same learner type in the distracted arm of Study 2. It quantified absolute and relative displacement in Mean Checklist Score, mean question-level run-to-run SD, and Mean Response Word Count. The analysis therefore measured how strongly the additional profile prompts disturbed learning outcomes, repeated-run stability, and response form.

\paragraph{SQ2---Matched-Ordering Preservation (Unfinished)}

\begin{quote}
\textbf{Are the matched learner-condition orderings observed in Study 2 preserved across the A1--B2 profiles?}
\end{quote}

SQ2 examined preservation of matched learner-condition ordering at question level. The Prompt branch used the ordering of Moderate- and High-Intensity Prompt-ADHD in the distracted arm of Study 2, while the CPB branch used the ordering of Low, Medium, and High constraints. The Ordering Consistency Rate (OCR) for each profile was compared with its matched Study 2 baseline. Tie rates were reported separately to distinguish strict preservation, preservation through equal scores, and reversal of the expected direction.

\paragraph{SQ3---Processing-Demand Sensitivity Preservation (Unfinished)}

\begin{quote}
\textbf{Does the relationship between source-round Processing Demand Bits and question performance change after the additional profiles are introduced?}
\end{quote}

SQ3 reused the Study 2 processing-demand analysis for the 42 Independent Questions that mapped uniquely to a source Teaching Round. The primary comparison was the change in the PDB--score slope between each Study 3 profile and the matched learner condition in the distracted arm of Study 2. Spearman rank correlation and the Low--High demand gap provided supporting evidence. SQ3 therefore assessed displacement in the previously observed WM/PDB-related behavioural sensitivity.

Study 3 did not independently validate whether the generated answers faithfully expressed the intended Language Ability or Big-Five constructs. Its conclusions were therefore restricted to \textit{robustness under the addition of Language Ability and Big-Five profile prompts}, rather than the accurate simulation of those learner traits.

\subsubsection{Experimental Design and Conditions (Unfinished)}

Study 3 used a learner-type $\times$ attribute-profile crossed design to test whether Prompt-based and CPB-based ADHD representations preserved their Study 2 outcomes, matched ordering, and processing-demand sensitivity after Language Ability and Big-Five characteristics were added. The design included two Prompt-ADHD learner types and three CPB learner types, as shown in Table~\ref{tab:study3-learner-types}.

\begin{table}[H]
\centering
\caption{Learner types included in Study 3.}
\label{tab:study3-learner-types}
\small
\begin{tabular}{p{0.24\textwidth}p{0.62\textwidth}}
\toprule
\textbf{Representation} & \textbf{Learner type} \\
\midrule
Prompt & Moderate-Intensity Prompt-ADHD \\
Prompt & High-Intensity Prompt-ADHD \\
CPB & CPB Low constraint \\
CPB & CPB Medium constraint \\
CPB & CPB High constraint \\
\bottomrule
\end{tabular}
\end{table}

The Prompt branch retained the Moderate- and High-Intensity ADHD descriptions from Study 2, and the CPB branch retained its Low, Medium, and High Attention/WM constraints. Prompt-NT and CPB Zero were not included. The results of these five learner types in the distracted arm of Study 2 served as matched baselines before the additional attributes were introduced.

\paragraph{Added Attributes and Representative Profiles (Unfinished)}

To control the experimental scope, Study 3 did not enumerate all Big-Five dimensions and high--low combinations. Building on the personality-aware student-simulation framework of Liu et al. \cite{liu-etal-2024-personality}, it selected Openness, Conscientiousness, and Neuroticism because they provided comparatively interpretable contrasts in the present learning context. Two Big-Five composite profiles were constructed. High Openness, High Conscientiousness, and Low Neuroticism formed the \textbf{learning-congruent BF} profile; Low Openness, Low Conscientiousness, and High Neuroticism formed the \textbf{learning-challenging BF} profile. These profiles supplied directionally explicit experimental contrasts and were not intended to exhaust the Big-Five personality space. The terms \textit{learning-congruent} and \textit{learning-challenging} were study-specific directional cue labels; they do not imply that Liu et al. established a causal beneficial or detrimental effect of these composites on learning.

The added Language Ability descriptions were also adapted from Liu et al. \cite{liu-etal-2024-personality}. High Language Ability encouraged complete and grammatically conventional sentence organisation, whereas Low Language Ability allowed more fragmented expression and some grammatical incompleteness. This attribute primarily controlled the organisation and form of the answer and did not directly represent the amount of knowledge acquired by the learner.

The two Big-Five composites were crossed with High and Low Language Ability to form four multidimensional learner profiles. This design tested the displacement produced by a limited set of representative profile-prompt additions without making the experiment intractable through exhaustive enumeration. Table~\ref{tab:study3-attribute-profiles} summarises the profiles.

\begin{table}[H]
\centering
\caption{Contrastive Language Ability and Big-Five profiles in Study 3.}
\label{tab:study3-attribute-profiles}
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.18}
\begin{tabular}{p{0.09\textwidth}p{0.19\textwidth}p{0.12\textwidth}p{0.18\textwidth}p{0.19\textwidth}p{0.16\textwidth}}
\toprule
\textbf{Group} & \textbf{Profile} & \textbf{Language} & \textbf{BF composite} & \textbf{Relationship} & \textbf{Role} \\
\midrule
A---Aligned & A1---Learning-Congruent Alignment & High & High O, High C, Low N & Both cues are directionally learning-congruent & Congruent aligned configuration \\
A---Aligned & A2---Learning-Challenging Alignment & Low & Low O, Low C, High N & Both cues are directionally learning-challenging & Challenging aligned configuration \\
B---Conflicting & B1---High Language / Challenging BF & High & Low O, Low C, High N & Language and BF cues point in different directions & Cross-attribute separability test \\
B---Conflicting & B2---Low Language / Congruent BF & Low & High O, High C, Low N & Language and BF cues point in different directions & Cross-attribute separability test \\
\bottomrule
\end{tabular}
\end{table}

A1 and A2 were aligned configurations in which Language Ability and Big-Five cues jointly pointed towards learning-congruent or learning-challenging characteristics, respectively. B1 and B2 were conflicting configurations in which the two cue types pointed in opposing directions. The four cells provided representative coverage of aligned and conflicting attribute relationships, but were analysed as complete profile-prompt additions rather than decomposed into separate main effects or an interaction.

\paragraph{Representation-Specific Attribute Placement (Unfinished)}

Both representations used the same Language Ability and Big-Five descriptions but placed them at different functional locations, as shown in Figure~\ref{fig:study3-attribute-placement}. The Prompt branch placed the ADHD description, Language Ability, and Personality Description jointly in the student prompt. The CPB branch continued to represent ADHD-related processing differences through its Attention and WM mechanisms and added Language Ability and Personality Description only at the memory-constrained answering stage. The added attributes therefore conditioned CPB answer generation while ADHD-related processing differences remained implemented by the existing CPB mechanisms.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,keepaspectratio]{Figures/Chapter3/export/ch03_rq3_prompt_structure_comparison.png}
    \caption{Placement of additional learner attributes in joint Prompt-ADHD and factorised CPB-ADHD representations.}
    \label{fig:study3-attribute-placement}
\end{figure}

In joint Prompt-ADHD, the ADHD description, Language Ability, Personality Description, and Core Student Instruction jointly conditioned answer generation from the complete frozen classroom context. In CPB-ADHD, ADHD-related differences were implemented through the Attention and Working-Memory mechanisms, while Language Ability and Personality Description were introduced only in the memory-constrained response-generation layer. Figure~\ref{fig:study3-attribute-placement} compares the profile-related prompt components rather than the complete payload; the unchanged Memory Restriction, Explicit LTM, and Question components are therefore omitted from the CPB side.

\paragraph{Experimental Procedure and Scale (Unfinished)}

Study 3 used only the same seven frozen distracted teaching materials as the distracted arm of Study 2; clean materials were not included. All learners therefore encountered the same ADHD-relevant instructional challenge, and every Study 3 condition could be compared with its matched Study 2 distracted baseline. The two Prompt-ADHD learner types and three CPB learner types were each crossed with A1, A2, B1, and B2, producing 20 learner-type--attribute-profile conditions. Each condition was run independently five times:

\[
20\ \text{conditions}
\times
5\ \text{runs}
=
100\ \text{simulated-learner runs}.
\]

Each run covered all seven teaching materials and answered all 49 frozen assessment questions, producing:

\[
100\ \text{runs}
\times
49\ \text{questions}
=
4{,}900\ \text{assessment answers}.
\]

Apart from the fixed use of distracted materials and the addition of Language Ability and Big-Five dimensions, Teaching-Round exposure, the student model, generation settings, 49 questions, five repetitions, and the Qwen3.7-Max criterion-level scoring procedure were the same as in Study 2. Prompt learners continued to answer from the complete round-wise conversation history, while CPB learners continued to answer from Explicit LTM. The principal added variable in Study 3 was therefore the attribute profile and its representation-specific placement.

Studies 2 and 3 formed a progressive rather than duplicative comparison. Study 2 crossed learner and material conditions to establish the behavioural baseline. Study 3 fixed the distracted materials and examined how outcomes, matched learner-condition ordering, and PDB sensitivity changed after A1--B2 profiles were added. This analytical relationship is summarised in Table~\ref{tab:study2-study3-relationship}.

\begin{table}[H]
\centering
\caption{Analytical relationship between Studies 2 and 3.}
\label{tab:study2-study3-relationship}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{p{0.15\textwidth}p{0.23\textwidth}p{0.25\textwidth}p{0.29\textwidth}}
\toprule
\textbf{Study} & \textbf{Row axis} & \textbf{Column axis} & \textbf{Main question} \\
\midrule
Study 2 / RQ2 & Learner condition & Clean / Distracted & Does learner representation respond systematically to distraction and demand? \\
Study 3 / RQ3 & Matched learner condition & Language $\times$ BF: A1 / A2 / B1 / B2 & How do outcomes, matched ordering, and PDB sensitivity change relative to the matched baseline? \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Evaluation Measures (Unfinished)}

Study 3 reused the question-level aggregation order, Checklist Score, sample SD, response word count, OCR, and processing-demand measures from Study 2 so that profile conditions could be compared directly with their matched distracted baselines. For question $q$, learner condition $c$, profile $p$, and run $j$, let the Checklist Score be $S_{q,c,p,j}$ and the response word count be $W_{q,c,p,j}$. With five runs per condition, question-level mean score, sample SD, and mean word count were:

\[
\bar S_{q,c,p}
=
\frac{1}{5}\sum_{j=1}^{5}S_{q,c,p,j},
\]

\[
SD_{q,c,p}
=
\sqrt{
\frac{
\sum_{j=1}^{5}
\left(S_{q,c,p,j}-\bar S_{q,c,p}\right)^2
}{5-1}
},
\]

\[
\bar W_{q,c,p}
=
\frac{1}{5}\sum_{j=1}^{5}W_{q,c,p,j}.
\]

All higher-level summaries first calculated these quantities at question level and then weighted questions equally within the relevant set. The primary analysis used all 49 questions and additionally reported the 42 Independent and seven Integrative Questions. The continuous PDB analysis for SQ3 used only the 42 Independent Questions that mapped uniquely to one source Teaching Round.

\paragraph{SQ1---Baseline-Relative Outcome Displacement (Unfinished)}

SQ1 quantified changes in Mean Checklist Score, mean question-level run SD, and Mean Response Word Count relative to the matched Study 2 distracted baseline. For outcome $X\in\{\mu,\overline{SD},\overline W\}$, let $X^{(2)}_c$ denote the Study 2 baseline for learner condition $c$, and let $X^{(3)}_{c,p}$ denote the result for the same condition under Study 3 profile $p$. Absolute and relative displacement were:

\[
\Delta X_{c,p}
=
X^{(3)}_{c,p}-X^{(2)}_c,
\qquad
R_{X,c,p}
=
\frac{X^{(3)}_{c,p}-X^{(2)}_c}{X^{(2)}_c}
\times100\%.
\]

The three overall quantities were:

\[
\mu_{c,p}
=
\frac{1}{Q}
\sum_{q=1}^{Q}\bar S_{q,c,p},
\qquad
\overline{SD}_{c,p}
=
\frac{1}{Q}
\sum_{q=1}^{Q}SD_{q,c,p},
\qquad
\overline W_{c,p}
=
\frac{1}{Q}
\sum_{q=1}^{Q}\bar W_{q,c,p}.
\]

SQ1 therefore reported $\Delta\mu$ and $R_{\mu}$, $\Delta\overline{SD}$ and $R_{SD}$, and $\Delta\overline W$ and $R_W$. A positive delta indicated that the Study 3 profile condition exceeded its matched baseline; a negative delta indicated a lower value. Mean Checklist Score displacement was the primary performance outcome, SD displacement described changes in repeated-run stability, and word-count displacement described response length only. It was not interpreted as knowledge gain, learning effectiveness, or attribute fidelity. If a baseline quantity equalled zero, the relative displacement was marked as not estimable rather than calculated using an arbitrary smoothing constant.

\paragraph{SQ2---Matched-OCR Preservation (Unfinished)}

SQ2 tested whether question-level ordering among the matched Study 2 learner conditions was preserved under A1--B2. Because Study 3 did not include Prompt-NT or CPB Zero, the Study 2 baselines were recalculated from the same learner subsets in the distracted arm rather than reusing the complete three-level Prompt OCR or four-level CPB OCR.

For the Prompt branch, the matched Study 2 baseline and profile-specific OCR were:

\[
OCR^{\mathrm{Prompt}}_0
=
\frac{
\#\left\{q:\bar S^{(2)}_{q,\mathrm{Moderate}}
\geq
\bar S^{(2)}_{q,\mathrm{High}}\right\}
}{Q},
\]

\[
OCR^{\mathrm{Prompt}}_p
=
\frac{
\#\left\{q:\bar S^{(3)}_{q,\mathrm{Moderate},p}
\geq
\bar S^{(3)}_{q,\mathrm{High},p}\right\}
}{Q}.
\]

For the CPB branch:

\[
OCR^{\mathrm{CPB}}_0
=
\frac{
\#\left\{q:\bar S^{(2)}_{q,\mathrm{Low}}
\geq
\bar S^{(2)}_{q,\mathrm{Medium}}
\geq
\bar S^{(2)}_{q,\mathrm{High}}\right\}
}{Q},
\]

\[
OCR^{\mathrm{CPB}}_p
=
\frac{
\#\left\{q:\bar S^{(3)}_{q,\mathrm{Low},p}
\geq
\bar S^{(3)}_{q,\mathrm{Medium},p}
\geq
\bar S^{(3)}_{q,\mathrm{High},p}\right\}
}{Q}.
\]

Both branches used non-strict ordering, so equal scores did not violate the expected direction. Profile-specific OCR change was reported in percentage points:

\[
\Delta OCR_{p,\mathrm{pp}}
=
100\left(OCR_p-OCR_0\right).
\]

To determine whether high OCR values were driven primarily by equal scores, SQ2 separately reported tie rates. For the Prompt branch:

\[
P^{\mathrm{Prompt}}_{\mathrm{tie},0}
=
\frac{
\#\left\{q:\bar S^{(2)}_{q,\mathrm{Moderate}}
=
\bar S^{(2)}_{q,\mathrm{High}}\right\}
}{Q},
\]

\[
P^{\mathrm{Prompt}}_{\mathrm{tie},p}
=
\frac{
\#\left\{q:\bar S^{(3)}_{q,\mathrm{Moderate},p}
=
\bar S^{(3)}_{q,\mathrm{High},p}\right\}
}{Q}.
\]

For the CPB branch, a tie required the overall non-increasing order to hold while at least one adjacent pair was equal:

\[
P^{\mathrm{CPB}}_{\mathrm{tie},p}
=
\frac{1}{Q}
\sum_{q=1}^{Q}
\mathbf 1\!\left[
\begin{aligned}
&\bar S_{q,\mathrm{Low},p}
\geq\bar S_{q,\mathrm{Medium},p}
\geq\bar S_{q,\mathrm{High},p}\\
&\land\left(
\bar S_{q,\mathrm{Low},p}=\bar S_{q,\mathrm{Medium},p}
\lor
\bar S_{q,\mathrm{Medium},p}=\bar S_{q,\mathrm{High},p}
\right)
\end{aligned}
\right].
\]

The Study 2 baseline $P^{\mathrm{CPB}}_{\mathrm{tie},0}$ used the same definition with distracted-arm baseline scores. Tie-rate change and the derived ordering composition were:

\[
\Delta P_{\mathrm{tie},p,\mathrm{pp}}
=
100\left(P_{\mathrm{tie},p}-P_{\mathrm{tie},0}\right),
\]

\[
P_{\mathrm{strict},p}=OCR_p-P_{\mathrm{tie},p},
\qquad
P_{\mathrm{reverse},p}=1-OCR_p.
\]

The primary SQ2 measures were therefore $OCR_p$, $P_{\mathrm{tie},p}$, $\Delta OCR_p$, and $\Delta P_{\mathrm{tie},p}$. Strict and reverse proportions were derived to explain the composition of ordering outcomes. Because Prompt and CPB imposed different numbers of ordering constraints, representational robustness was not inferred from their raw OCR values directly; the principal comparison concerned change relative to each matched baseline. Floating-point equality was evaluated using a fixed numerical tolerance so that each question was assigned to exactly one of the strict, tie, or reverse categories.

\paragraph{SQ3---Processing-Demand Sensitivity Preservation (Unfinished)}

SQ3 tested how much the relationship between performance and source-round PDB changed after A1--B2 profiles were added. The analysis used only the 42 Independent Questions because each mapped to one source Teaching Round; the seven Integrative Questions involved multiple rounds and had no unique single-round PDB.

For learner condition $c$, the Study 2 baseline model was:

\[
\bar S^{(2)}_{q,c}
=
\alpha^{(2)}_c
+
\beta^{(2)}_{c,\mathrm{PDB},100}
\left(\frac{PDB_q}{100}\right)
+
\varepsilon_q,
\]

and each Study 3 profile model was:

\[
\bar S^{(3)}_{q,c,p}
=
\alpha^{(3)}_{c,p}
+
\beta^{(3)}_{c,p,\mathrm{PDB},100}
\left(\frac{PDB_q}{100}\right)
+
\varepsilon_q.
\]

Here, $\beta_{\mathrm{PDB},100}$ denoted the estimated change in Mean Checklist Score for each 100-bit increase in source-round PDB. The primary measure was slope displacement:

\[
\Delta\beta_{c,p}
=
\beta^{(3)}_{c,p,\mathrm{PDB},100}
-
\beta^{(2)}_{c,\mathrm{PDB},100}.
\]

$\Delta\beta\approx0$ indicated that the PDB--score relationship remained close to the matched baseline. A positive value indicated a shift towards a more positive slope, and a negative value indicated a more negative slope. Because the sign described only the direction of change, robustness was judged primarily from the magnitude of $|\Delta\beta|$ and its consistency across A1--B2.

As supporting measures that did not rely on a linear model, SQ3 also calculated Spearman's rank correlation $\rho$ between PDB and question-level Mean Checklist Score, together with the predefined performance gap between Low- and High-demand Independent Questions:

\[
G_{c,p}
=
\bar S_{\mathrm{Low\ demand},c,p}
-
\bar S_{\mathrm{High\ demand},c,p},
\]

\[
\Delta\rho_{c,p}=\rho^{(3)}_{c,p}-\rho^{(2)}_c,
\qquad
\Delta G_{c,p}=G^{(3)}_{c,p}-G^{(2)}_c.
\]

PDB--score slope displacement was the primary SQ3 evidence; $\Delta\rho$ and $\Delta G$ tested whether the conclusion depended on the linear fit or a single summary. SQ3 evaluated preservation of WM/PDB-related behavioural sensitivity and did not treat the PDB slope as a direct measurement of the Working-Memory mechanism itself. Table~\ref{tab:study3-evaluation-framework} summarises the three subquestions and measures.

\begin{table}[H]
\centering
\caption{Evaluation framework for Study 3.}
\label{tab:study3-evaluation-framework}
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.18}
\begin{tabular}{p{0.22\textwidth}p{0.25\textwidth}p{0.23\textwidth}p{0.22\textwidth}}
\toprule
\textbf{Subquestion} & \textbf{Core comparison} & \textbf{Primary measures} & \textbf{Supporting evidence} \\
\midrule
SQ1---Outcome displacement & Study 3 profile vs matched Study 2 distracted baseline & $\Delta\mu$, $R_\mu$, $\Delta\overline{SD}$, $R_{SD}$ & $\Delta\overline W$, $R_W$; Independent/Integrative summaries \\
SQ2---Matched-OCR preservation & Matched Study 2 OCR vs A1--B2 profile OCR & $OCR_p$, $\Delta OCR_p$, $P_{\mathrm{tie},p}$, $\Delta P_{\mathrm{tie},p}$ & Strict/tie/reverse composition; Independent/Integrative summaries \\
SQ3---Demand-sensitivity preservation & Matched Study 2 PDB relationship vs A1--B2 profile relationship & $\Delta\beta_{\mathrm{PDB},100}$ & $\Delta\rho$, $\Delta G_{\mathrm{Low-High}}$ \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Expected Patterns and Interpretation (Unfinished)}

Study 3 followed the SQ1--SQ3 sequence to examine preservation of outcomes, learner-condition ordering, and processing-demand sensitivity relative to the matched Study 2 distracted baselines after multidimensional profile prompts were introduced. All expectations concerned aggregate patterns. Apart from the question-level OCR classification, the study did not require every question, lesson, or run to exhibit the same relationship, nor did it assume that final scores must remain completely unchanged.

\begin{table}[H]
\centering
\caption{Prespecified aggregate-level expectations for Study 3.}
\label{tab:study3-expectations}
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.18}
\begin{tabular}{p{0.27\textwidth}p{0.65\textwidth}}
\toprule
\textbf{Subquestion} & \textbf{Prespecified aggregate-level expectation} \\
\midrule
SQ1---Outcome displacement & A representation robust to the additional profiles should show generally small $|\Delta\mu|$ and $|\Delta\overline{SD}|$ across A1--B2 rather than displacement driven by a single profile. $\Delta\overline W$ describes response-length change and is not direct evidence of learning-performance robustness. \\
SQ2---Matched-OCR preservation & Preservation of matched learner-condition ordering should produce $\Delta OCR$ and $\Delta P_{\mathrm{tie}}$ values close to zero without a systematic increase in reversals. OCR must be interpreted with its tie composition so that equal scores are not mistaken for strict ordering. \\
SQ3---Demand-sensitivity preservation & A robust PDB--performance relationship should produce a small $|\Delta\beta_{\mathrm{PDB},100}|$, supported by directionally consistent $\Delta\rho$ and $\Delta G_{\mathrm{Low-High}}$. Larger slope displacement indicates that the additional profiles changed the original demand sensitivity. \\
\bottomrule
\end{tabular}
\end{table}

For SQ1, $\Delta\mu$ and $\Delta\overline{SD}$ described absolute changes in mean performance and repeated-run variability; relative displacement supplemented these quantities across different baseline scales. Because Language Ability could directly alter response length, $\Delta\overline W$ was interpreted only as response-form displacement. For SQ2, Prompt and CPB imposed different numbers of ordering constraints, so their raw OCR values were not compared directly; the principal evidence was each branch's $\Delta OCR$ and strict/tie/reverse composition relative to its own baseline. For SQ3, the PDB slope, rank association, and Low--High gap used the same 42 Independent Questions to avoid ambiguity from the multi-round source structure of Integrative Questions.

Aligned and conflicting profiles were not assumed to be simple beneficial or detrimental learner types, and were not treated as an additional independent statistical factor. A1, A2, B1, and B2 were each compared with the matched baseline as complete profile-prompt additions. Observed displacement was not decomposed into independent causal effects of Language Ability, Big-Five traits, or their interaction.

The overall RQ3 judgement integrated three forms of baseline-relative evidence: outcome displacement, matched-ordering preservation, and PDB-sensitivity displacement. If CPB showed smaller and more consistent absolute displacement than Prompt across A1--B2 while preserving its matched OCR and PDB--performance relationship more closely, this would support greater multidimensional representational robustness for factorised CPB under the additional profile prompts. If the evidence differed across levels, the results would be reported separately rather than reduced to a conclusion based on one average score. This claim remains restricted to robustness under the addition of Language Ability and Big-Five profile prompts and does not establish that those traits were accurately simulated.

\section{Implementation, Reproducibility and Ethical Scope}

\mbox{}\par

\subsection{Models and Generation Settings}

\mbox{}\par

\subsection{Experiment Execution and Reproducibility}

\mbox{}\par

\subsection{Statistical Uncertainty and Repetition Conventions}

\mbox{}\par

\subsection{Ethical Scope and Interpretation}

\mbox{}\par
